# sonic 仓库协作硬约束

## 内容与事实

- 仓库维护三条主线：`文档/Harness体系/` 讲通用知识，`文档/sonic设计/` 讲本产品目标与取舍，`代码/sonic/` 放可运行实现与测试，`面经/` 独立维护题库与周报。根目录只放入口、协作、许可证和安全等元文件。
- 产品名统一为 `sonic`；旧名 TraceForge 只允许出现在历史迁移说明或历史提交中。面经不是项目设计的唯一正文；设计事实以 `文档/sonic设计/` 为准。
- 代码能力只允许标记为 `已实现`、`设计中`、`待验证`。称为 `已实现` 必须同时能指向代码与测试。设计文档、README、面经不得把目标架构当成已有功能，也不得虚构生产流量或评测提升。
- Harness 技术事实标明公开来源及核验日期；外部项目只描述可核实的公开行为，不推断未公开内部机制。面经线索保留原始链接、日期与可信度，不冒充企业官方题库或频率统计。
- `Harness体系`、`sonic设计`、`面经` 的职责不重复：概念原理放体系文档，产品决定放设计/ADR，问题与答案放面经。跨线使用链接，不复制大段正文。

## 代码与安全

- 运行内核只掌管通用请求、状态、模型提案、受控执行、观察、预算、恢复和终态；任务阶段与完成条件由能力配置提供，不把 RepoFix 写死进核心。
- 模型输出不是执行许可。工具调用先校验输入与权限；高风险写入需明确授权。外部操作结果不明时先对账，不盲重试。完成须按任务契约核验证据，不能仅凭模型自述。
- 公开仓库不提交 API Key、真实用户会话、个人记忆、凭据或敏感数据。不保存与源码重复的生成 ZIP。新依赖、联网、外部写操作和自动化权限必须有具体用途与风险说明。
- 保留现有测试和教学行为；改变事件、完成判定或副作用语义时，补失败场景测试，并同步能力状态表与相关设计。

## Git 与交付

- 每次修改前获取 `https://github.com/ggb-pro/agent-harness-course` 的最新 `origin/main`，检查工作树；不要用聊天附件或旧本地副本覆盖远端内容。保留历史与已有内容，迁移时修复链接；删除材料需说明去向。
- 在 `codex/...` 分支做聚焦修改；代码、设计文档与每周面经尽量分开提交。适用检查通过后推送到 GitHub，并读取远端提交核验。对于仍未配置保护规则的 `main`，也应使用 PR 与 CI 合入，不强推或覆盖历史。
- PR 说明目的、能力状态、验证、文档影响、风险与撤回方法。推送或远端核验失败时明确报告“仅本地提交、尚未同步 GitHub”、提交号和原因，不宣称完成上传。
- 每周面经自动化只生成候选/草稿，不直接合入 `main`；人工核对来源、去重和版权后才发布。GitHub Actions 令牌只授予工作流实际需要的权限。
