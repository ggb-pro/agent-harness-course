# 贡献与同步流程

所有工作以最新 GitHub `origin/main` 为基准，不从聊天附件或旧导出覆盖仓库。先 `git fetch origin`、检查 `git status`，必要时更新干净的本地 `main`；有未提交用户改动时先保留和避让。

## 一次修改的最小闭环

1. 建 `codex/<主题>` 分支，改一个聚焦问题；迁移材料时保留历史、修复旧链接。
2. 若代码语义变化，更新测试、[能力状态表](./文档/sonic设计/02-能力状态.md)及受影响设计；知识资料改变时记录来源与核验日期；面经更新同时维护周报与来源。
3. 运行 `代码/sonic` 的 29 项基线测试及本仓库适用检查。真实模型评测可另跑，但不能拿 FakeProvider 测试冒充真实任务成绩。
4. 提交、推送分支、建 PR。PR 写清目的、能力状态、验证、文档影响、风险和撤回方法；CI 通过并复核后合入 `main`，再读取远端提交确认同步。

建议提交前缀：`feat:`、`fix:`、`docs:`、`interview:`、`chore:`。代码、设计、面经不必硬拆成多个 PR，但每个 PR 应有一个清楚的主目的。不要强推 `main` 或删除用户历史。推送失败时只报告本地完成和提交号。

## 文档与面经的验收

新知识文档先用通俗例子解释“为什么”，再讲机制、边界、代码对照和来源。新设计决定写 ADR；“已实现”必须有代码与测试。面经新增题使用稳定题号，不根据公开讨论推断未经核实的企业频率；周报只记录变化，不复制整份答案。
