# 学习文档：跟着一次修 Bug，做懂 Agent Harness

你有一个会回答编程问题的模型。现在希望它打开项目，找到 Bug，改代码，跑测试，失败后继续修。中间缺的这一层工程，就是 Harness。

本课程围绕一个小问题连续展开：订单金额以“分”保存，打八折后却放大了一百倍。我们让同一个 Mini Sonic 先看到失败，再读代码、修改、重测，接着人为制造冲突、拒绝、超时和重启。每增加一个机制，都先说明原来的系统在哪里失效。

这里有三种内容，读的时候请分清：

- **实验**：仓库里的 Python 程序，离线可运行，测试和文件修改都真实发生在临时目录。
- **机制**：从实验推广到真实模型时，协议和工程上还需要补齐的部分。
- **Sonic 决策**：集中放在[设计说明](./设计说明.md)，其中标记的产品能力仍需开发。

Mini Sonic 使用规则写死的假模型，目的是稳定地观察执行过程。它不能自主解决新 Bug，实验通过也不代表真实模型任务成功率。真实 Agent 的思考来自模型，下面要学的是怎样接住模型的思考并让行动可靠发生。

## 阅读路线

| 想解决的问题 | 从这里进入 |
| --- | --- |
| Agent 到底比聊天多了什么 | [一：让回答变成行动](#一让回答变成行动) |
| 怎样亲手跑通一次修复 | [二：运行同一个实验](#二运行同一个实验) |
| Tool Calling 到底交换什么 | [三：模型和工具怎样接上](#三模型和工具怎样接上) |
| 怎么避免改错文件、误信测试 | [四：读过再改](#四读过再改) · [五：真正完成](#五真正完成) |
| 为什么要上下文、会话和恢复 | [六：上下文](#六上下文为什么越来越难用) · [七：恢复](#七继续对话和恢复执行是两件事) |
| Claude Code 的机制如何映射到实现 | [八：流式交互](#八流式输出和用户介入) · [九：参考实现](#九带着问题读参考实现) |
| 怎样从个人编码工具走向通用助手 | [十：扩展](#十从编码助手扩展出去) · [十一：评测](#十一怎样知道真的变好了) |

初学者顺读前七章。每章运行一条命令，再回头读解释；有经验的读者优先看协议配对、过期证据、恢复窗口和第九章的源码阅读路径。

## 一：让回答变成行动

### 1. 一个回答为什么不够

项目中的函数是：

```python
def total(cents, percent):
    return cents * (100 - percent)
```

需求规定：金额和折扣都是整数，结果仍以“分”表示，不足一分向下取整。因此 `total(10000, 20)` 应返回 `8000`。模型很可能看一眼就建议补上 `// 100`。

但用户要的是仓库里的 Bug 被修好。文本建议还没有回答：究竟改哪份文件？磁盘上的代码是否和模型看到的一样？用户是否允许修改？相关测试通过了吗？期间进程退出怎么办？

把这些问题交给应用程序，才有下面的闭环：

```text
模型看到任务与证据
  → 提出一个工具调用
  → 程序检查并执行
  → 得到新的事实
  → 把事实放进下一次模型请求
  → 模型决定继续还是交付
```

Claude Code 官方将这种工作方式概括为收集上下文、行动、验证的反复循环。这里的阶段可以交错，修复失败后重新阅读很正常。[工作机制](https://code.claude.com/docs/en/how-claude-code-works)

### 2. 一个可以直接运行的最小循环

把整个代码块复制进 Python 3.10+，不需要安装包：

```python
# runnable: minimal_loop
def model(messages):
    if messages[-1]["role"] == "tool":
        return {"text": "观察到：" + messages[-1]["content"]}
    return {"call": {"id": "c1", "name": "read", "args": {}}}

def read():
    return "def total(cents, percent): return cents * (100 - percent)"

tools = {"read": read}
messages = [{"role": "user", "content": "看看金额计算哪里有问题"}]

for turn in range(3):
    reply = model(messages)
    messages.append({"role": "assistant", **reply})
    if "call" not in reply:
        print(reply["text"])
        break
    call = reply["call"]
    fn = tools[call["name"]]
    result = fn(**call["args"])
    messages.append({
        "role": "tool",
        "call_id": call["id"],
        "content": result,
    })
else:
    raise RuntimeError("轮数耗尽")
```

输出应以“观察到：def total”开头。注意：`model` 是假模型，它甚至没有推理，只根据最后一条消息选分支。这个极简程序揭示了一个重要事实：每次模型调用结束后，下一步由宿主程序接手；工具并不在模型内部执行。

循环中的三件事值得反复看：保留模型提出的调用、执行工具、用相同调用 ID 返回结果。真实模型服务换的是消息编码与推理过程，这三件事仍然成立。

Sonic 旧原型的同类路径在 [AgentRunner.run](./代码/sonic/src/sonic_agent/core/runner.py)，其中还能看到次数限制和取消检查。阅读时先找到追加 assistant 和 tool 消息的位置，再研究错误分支。

## 二：运行同一个实验

### 1. 十分钟看到完整过程

从仓库根目录执行：

```powershell
python 代码/sonic/examples/mini_sonic.py --case normal
```

适用 Python 3.10+，Windows 和 Linux 都使用同一命令；Linux 如果只有 `python3`，替换可执行名即可。这个实验只用标准库，不读取 API Key，也不会修改当前项目。它会创建临时目录，写入 `pricing.py` 和三个测试，在退出时清理这些实验文件。

预期输出：

```text
tools: test -> read -> edit -> test
status: verified
errors: []
experiment: PASS
```

完整实现只有一个文件：[mini_sonic.py](./代码/sonic/examples/mini_sonic.py)。阅读顺序是 `experiment → Lab.run → ScriptedModel.complete → Lab.execute → Lab.verification`。

| 步骤 | 新观察 | 下一步为什么改变 |
| --- | --- | --- |
| 第一次 test | 3 个用例中有失败，退出码 1 | 需要看实现 |
| read | 返回原始文本及内容哈希 | 现在知道基于哪个版本修改 |
| edit | 保存旧内容，写入修复，返回新哈希 | 修改成功还需要验证 |
| 第二次 test | 3 个用例通过，退出码 0 | 可以报告这些用例已通过 |
| verification | 测试对应的哈希仍是当前文件 | 证据没有因后续改动而过期 |

这条路径由假模型控制，修复文本也是预先给定的。文件、子进程、退出码、快照和日志则是真实的。这种分工让你不花 API 费用就能稳定复现 Harness 的行为。

### 2. 找到主循环的四行

下面是实验中的核心语句节选，不能作为单独脚本运行：

```python
reply = model.complete(self.messages)
self.record("message", {"role": "assistant", **reply})
result = self.execute(call)
self.record("message", {"role": "tool", "call_id": call["id"],
                        "name": call["name"], "result": result})
```

`record` 同时把消息追加到内存和 JSONL 文件。下一轮假模型能看到结果，新进程也能重建历史。这正是模型交互、执行和记录三个职责相遇的地方。

实验故意只有一个可编辑文件和一个固定测试命令，因此没有通用路径解析或任意 Shell。它也没有通用 Schema 校验、交互审批、完整事务和 OS 沙箱。这些简化让我们先看清一条完整链路，生产设计需要逐项补齐。

### 3. 把失败也跑一遍

```powershell
python 代码/sonic/examples/mini_sonic.py --case false_finish
python 代码/sonic/examples/mini_sonic.py --case denied
python 代码/sonic/examples/mini_sonic.py --case stale
python 代码/sonic/examples/mini_sonic.py --case timeout
python 代码/sonic/examples/mini_sonic.py --case resume
python 代码/sonic/examples/mini_sonic.py --case context
```

前四项应为 `unverified`；后两项应为 `verified`。每项最后都应输出 `experiment: PASS`，它表示“实验的预期被满足”，所以正确拒绝修改也会 PASS。

后面的章节解释这些实验分别证明什么，以及哪些更强的结论不能由它们推出。

## 三：模型和工具怎样接上

### 1. 模型需要一份工具说明书

模型不会自动知道 Python 里有哪些函数。请求里必须提供工具名、描述和参数结构。一个读文件工具可以这样描述：

```json
{
  "name": "read",
  "description": "读取当前工作区的 UTF-8 文本，返回行号和文件内容哈希",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {"type": "string"},
      "start_line": {"type": "integer", "minimum": 1},
      "max_lines": {"type": "integer", "minimum": 1, "maximum": 200}
    },
    "required": ["path"],
    "additionalProperties": false
  }
}
```

描述影响模型是否选对工具，Schema 帮助检查参数形状，执行器决定具体做什么。三个地方必须一起维护。把 `path` 改为 `file` 却不改 Schema，会使能力看起来可用、实际始终报错。

给所有工具都起“处理数据”这样的名字也不行。工具接口是模型使用的用户界面：用途越清楚，模型越少需要试错。

### 2. 一次真实工具交换

以下是 Anthropic Messages 的简化线序列，省略 HTTP 字段：

```json
[
  {"role":"user","content":"查看 pricing.py"},
  {"role":"assistant","content":[
    {"type":"tool_use","id":"c1","name":"read",
     "input":{"path":"pricing.py"}}
  ]},
  {"role":"user","content":[
    {"type":"tool_result","tool_use_id":"c1",
     "content":"1: def total(cents, percent): ..."}
  ]}
]
```

这个协议把工具结果放在 user 消息的 `tool_result` 块里；教学代码中的 `role=tool` 是自己的内部表示。接入供应商时，适配器负责转换。不要把一种 API 的消息结构硬套到另一种 API。[工具结果协议](https://platform.claude.com/docs/en/agents-and-tools/tool-use/handle-tool-calls)

如果模型同一轮请求 `c1=read(pricing.py)` 和 `c2=read(test_pricing.py)`，每个调用都需要对应的结果。一个被拒绝，也要返回带相同 ID 的错误结果。凭空删除失败调用会让模型误以为它还没执行，或者直接产生不合法历史。

### 3. 流式参数为什么不能边收边执行

工具参数可能分片到达：

```text
片段 1：{"path":"pric
片段 2：ing.py","max_lines":
片段 3：40}
块结束
响应结束：tool_use
```

片段 1 不是完整 JSON。即使片段 3 恰好可解析，也还需要确认整个响应正常结束、调用 ID 唯一、参数合法。

Sonic v0.1 的选择是：文字可以边接收边展示；工具必须等整条响应完成并保存后才执行。它牺牲一点工具启动时延，却让断流和批量调用的语义容易验证。供应商协议确实区分块增量、块结束和消息结束。[Streaming messages](https://platform.claude.com/docs/en/build-with-claude/streaming)

### 4. Provider 应该翻译到什么程度

好的适配器把网络错误、文字增量、完整工具调用和用量变成系统统一表示；同时保留恢复所需的原始供应商内容块。某些协议有 opaque 数据或签名内容，不能只留下“用户看得见的文字”就宣称无损恢复。

推理文字是否可见取决于服务协议。程序应展示供应商允许展示的内容，不伪造、推导或依赖隐藏推理。调试时优先记录模型实际输入、工具调用、结果和终止原因。

## 四：读过再改

### 1. 为什么必须记住文件版本

想象模型读到旧代码后，用户在编辑器里修了一半。模型随后拿旧版本生成的补丁覆盖当前文件，用户的工作可能消失。

运行：

```powershell
python 代码/sonic/examples/mini_sonic.py --case stale
```

实验在读取与编辑之间追加一行用户注释。预期看到 `conflict`，并验证用户注释仍存在。修改被拒绝不是 Agent 变笨，而是证据已过期，需要重新读取。

代码中的关键判断：

```python
raw = path.read_bytes()
if digest(raw) != args["expected_hash"]:
    return {"ok": False, "error": "conflict"}
```

哈希是内容指纹，回答“我读到的版本和现在一样吗”。它不自动解决竞态：检查后、写入前仍可能有外部进程修改文件。实验只有一个 writer；产品需要串行自己的写入、缩短检查窗口、保存快照，并明确对同权限外部编辑仍采用冲突检测而非绝对隔离。

### 2. 一次编辑需要留下什么

至少留下调用 ID、路径、修改前哈希、修改后哈希、旧内容快照和工具结果。旧内容保存成功后再替换目标文件；替换通过同目录临时文件完成，避免把半个文件暴露出来。

单文件原子替换不意味着多文件事务。跨两个文件的补丁在第二个文件失败时可能已经改了第一个。首版可以只允许一次编辑一个文件，把每个结果明确反馈给模型。更复杂的原子补丁需要另行设计，不能只在文档里写“支持原子性”。

### 3. 撤销也要检查版本

若用户在 Agent 修改后又手工编辑，直接恢复旧快照仍会覆盖用户工作。撤销必须先检查当前哈希等于 Agent 写入后的哈希；不一致时展示差异，由用户决定如何保留。

Claude Code 的文件检查点提供了有用的产品参考，但官方明确说明通过 Shell 造成的修改不能全部靠 rewind 恢复。[Checkpointing](https://code.claude.com/docs/en/checkpointing)

### 4. 权限拒绝怎样进入推理

```powershell
python 代码/sonic/examples/mini_sonic.py --case denied
```

这次模型照样提出 edit，但执行层返回 `permission_denied`，磁盘保持原样。实验用固定策略拒绝，没有弹出审批框。

真实 Agent 可以改为给补丁建议、解释需要的权限，或等待用户变更任务范围。它不能把拒绝当作工具没找到，再换 Shell 绕过去。

这里需要分清三件事：Prompt 告诉模型该怎么做；权限规则决定一次调用能否运行；沙箱限制进程实际能碰到什么。允许测试命令也不代表测试安全——测试本身会执行仓库代码。

旧 Sonic 对普通文件读取的边界实现可以直接研究：[WorkspaceScope](./代码/sonic/src/sonic_agent/capabilities/repository.py)及[路径测试](./代码/sonic/tests/test_repository_tools.py)。注意这些工具没有 OS 沙箱能力。

## 五：真正完成

### 1. 模型停止说话和任务完成

运行：

```powershell
python 代码/sonic/examples/mini_sonic.py --case false_finish
```

假模型第一轮就说“Done!”，没有工具调用。预期 `unverified`。这个最小反例说明：没有 tool calls，只能说明这一轮模型没有再提出动作。

反过来，有一次 exit code 0 也不够。它可能来自无关命令、没有发现任何测试的测试器，或者修改代码之前的一次通过。

### 2. 把验证绑定到产物

实验的 verification 查找最后一次测试，并比较记录的文件哈希与当前哈希：

```python
passed = bool(tests and tests[-1]["exit_code"] == 0
              and tests[-1]["hash"] == current)
```

这只适用于实验的单文件目标和固定测试脚本。真实项目至少要把证据绑定到工作树版本、准确命令、工作目录、退出码、测试发现结果和日志引用。

验证有两个不同层次：

- **可以机械判定的部分**：是否执行了命令、是否通过、证据是否对应当前版本、是否改了禁止路径。
- **需要语义判断的部分**：修复是否符合需求、测试是否足够、有没有更合适的设计。

Verifier 能防止明显的错误交付，不能成为一个“自动证明任意程序正确”的黑盒。Sonic 应把检查状态和语义结论分别呈现。

### 3. 失败怎样推动下一步

测试失败不一定要结束 Run。它通常是有价值的新观察：断言失败指向逻辑问题，导入失败指向环境问题，零测试发现指向命令或路径问题。

真实模型应该看到足够的信息来区分它们。统一返回“命令失败，请重试”会诱发重复执行；返回命令、cwd、退出码和关键错误片段才有助于修正。

运行超时实验：

```powershell
python 代码/sonic/examples/mini_sonic.py --case timeout
```

这里运行的是固定睡眠子进程，超时后由 Python 结束并回收，结果为 `timeout`。它没有验证任意进程树的取消，也没有证明取消能撤销已发生的文件修改。

### 4. 一个诚实的交付

本例的交付可以是：

> 已把金额计算改为除以 100 后向下取整。固定样例的折扣、零折扣、全折扣三项测试通过，测试对应当前文件。尚未验证负金额和非法折扣输入。

“还有什么没验证”帮助用户判断适用范围。一个醒目的绿色完成标记不应掩盖这些边界。

## 六：上下文为什么越来越难用

### 1. 第十次读取以后，模型看到了什么

模型每次请求的可见输入，称为 Context。它可能包含规则、工具定义、用户目标、旧对话、文件内容和命令输出。早期几十行代码很清楚，十次测试后却可能被几万行日志占满。

代价包括费用、时延、窗口溢出，以及重要约束难以被注意到。工程上首先要问“哪些信息还需要保留”，而不是立刻增加向量数据库。

### 2. 先删冗余，再做语义摘要

运行：

```powershell
python 代码/sonic/examples/mini_sonic.py --case context
```

`compact_observations` 复制消息视图，把测试输出正文换成哈希引用，保留命令结果和调用 ID。实验会显示字符数下降，并断言原用户约束和结果配对仍存在。

这里的哈希只用于在原实验日志中定位正文；不是已经实现的 Artifact 服务。字符数也不等于 token 数。这个实验验证的是“可以减少重复证据，且不破坏消息结构”，没有调用模型生成摘要。

下一步才考虑让模型概括旧工作：

```json
{
  "goal": "修复折扣金额",
  "constraints": ["不能修改测试"],
  "facts": [{"text": "正常折扣被放大100倍", "source": "test:c1"}],
  "changes": [{"path": "pricing.py", "after_hash": "..."}],
  "checks": [{"command": "python -B test_pricing.py", "exit_code": 0}],
  "open_questions": ["非法折扣如何处理"],
  "failed_attempts": []
}
```

这是一份摘要格式示例。约束最好从权威记录重新装配，代码哈希从工作区读取；让摘要模型自行“回忆”这些字段会引入失真。

### 3. 上下文有三个容易踩的坑

**破坏工具配对。** 只删 assistant 调用、保留 tool result，会生成孤立结果。裁剪应该以完整交互块为单位。

**把旧观察当成当前事实。** 文件读取以后发生修改，历史正文仍有解释价值，却不再是可以直接编辑的版本。

**压缩后立刻又超限。** 一个新工具输出比整个预算还大，反复总结解决不了问题。必须在工具层限制输出，正文留在本地按需读取。

稳定系统规则和工具说明尽量放在稳定前缀，有助于支持前缀缓存的模型服务。缓存能减少重复计算成本，但不能减少模型需要理解的信息量；缓存策略也必须按供应商核验。

### 4. Memory 为什么另算

Session 摘要回答“刚才做到哪”；项目指令回答“这个项目按什么规则开发”；长期 Memory 回答“跨任务哪些偏好值得保留”。把每次测试输出都变成长期记忆，只会积累噪声和过时事实。

旧 Sonic 的 [ContextBuilder](./代码/sonic/src/sonic_agent/core/context.py)可用于观察优先级选择，不过它尚未接入旧 Runner，必保留项超预算时也没有完整产品处理。

## 七：继续对话和恢复执行是两件事

### 1. 恢复完整历史的简单情况

运行：

```powershell
python 代码/sonic/examples/mini_sonic.py --case resume
```

实验在编辑成功、工具结果已经记入日志后停止循环，启动第二个 Python 进程读取日志，再执行测试。它断言 edit 只出现一次。

这是一次**干净边界的跨进程恢复**。它证明 JSONL 中的历史足以让假模型接着走，不证明断电恢复或外部动作去重。

Session 是多次用户交流的容器；Run 是一次用户请求的执行；一次 Run 可以包含多个模型往返。恢复对话通常创建新 Run，继承历史；恢复未完成动作则需要判断上一 Run 到底做到哪里。

### 2. 只差一行日志，含义可能完全不同

| 崩溃位置 | 磁盘可能发生什么 | 恢复应依据什么 |
| --- | --- | --- |
| edit 意图保存前 | 尚未写入 | 可以重新规划 |
| 意图保存后、写入前 | 旧内容仍在 | 比较 before 哈希 |
| 写入后、结果入库前 | 文件已变，日志却说运行中 | 比较 before/after 哈希和快照 |
| 测试启动后、结果入库前 | 子进程可能仍运行 | 进程身份、终止情况和产物状态 |
| 结果入库后 | 完整结果可用 | 重放观察，不重复执行 |

SQLite 的事务只能覆盖数据库更新，不能让数据库与任意文件系统或网络服务成为同一个事务。文件变更可通过内容哈希对账，发邮件或部署则需要目标系统提供查询或幂等机制。

`EffectJournal` 这样的名字本身不提供保证。旧 Sonic 的[内存实现](./代码/sonic/src/sonic_agent/capabilities/tools.py)在进程重启后就没有记录了；读源码时要追问存在哪里、何时写入、如何对账。

### 3. 会话存储为什么要有两种视图

用户和模型需要易读的对话；系统需要开始、完成、拒绝、取消等执行事实。可以把两者关联起来，但不能用模型最终一句“都做完了”覆盖工具的失败记录。

Sonic 旧原型的 [SqliteEventStore](./代码/sonic/src/sonic_agent/core/events.py)已经有本地事务和 revision 检查；[现有测试](./代码/sonic/tests/test_core_contract.py)证明部分存储性质。跨进程工作恢复还需要上表中的动作协议，不能只凭数据库存在就宣称支持 resume。

## 八：流式输出和用户介入

### 1. 用户等的是什么

模型生成文字、工具运行、审批等待是三种不同的等待。CLI 应显示正在发生什么：“搜索文件”“运行测试，已用时 12 秒”“等待批准修改 pricing.py”。这些状态来自事件，不需要展示模型隐含推理。

完整终端界面只是表达层。它不应在渲染一行“已批准”时顺带改变权限数据库；批准必须通过明确的控制事件进入运行时。

### 2. 插话发生在什么边界

模型一次提出三个工具调用，执行完第一个后用户说“别改代码，先解释”。系统要在下一动作前检查新消息，丢弃尚未开始的写动作，给那些调用补上取消结果，再让模型看到新指令。

等待下一次整轮模型调用结束才处理用户输入，往往已经太晚。让旧工具队列照常执行再追加“用户不想改了”的文字，也不满足用户意图。

### 3. 并行的边界

两个独立 read 可以并行；同一文件的 edit 必须排序；测试应等待相关编辑结束。第一版全串行通常值得接受，因为日志顺序、取消和写冲突更容易解释。优化时应先测时延构成，确认瓶颈是否真在工具调度。

Subagent 是更大粒度的委托，它带来独立上下文和额外模型成本。它适合独立调查，不能绕过主任务权限，也不会免除主 Agent 的整合责任。

## 九：带着问题读参考实现

本节记录 2026-09-18 的核验。Claude Code 部分依据官方行为与协议资料；本次没有取得并核验泄露或反编译包，所以不声称已掌握它的私有内部函数。源码分析使用下面两个可定位提交的公开项目。

### 1. Claude Code：从观察建立可测试的行为

从一个具体操作入手：让它读取文件、提出修改、运行测试，观察工具结果、权限提示、最终报告；再中断、继续或撤销。记录输入、产品版本和可观察结果，区分“看到了什么”和“猜测内部怎样实现”。

例如，“恢复后仍能看到历史”不能推导出“每个工具都支持恰好一次执行”。“能撤销编辑”也不能推导出“Shell 副作用可回滚”。[工作机制](https://code.claude.com/docs/en/how-claude-code-works)与[检查点限制](https://code.claude.com/docs/en/checkpointing)给出了可以复查的范围。

如果后续研究发布包或反编译代码，应记录包版本、哈希、入口函数和对应行为实验。找不到明确版本的截图不作为 Sonic 接口决策的依据。

### 2. nanobot：沿着一次模型响应读

固定阅读入口：[runner.py @ 24c41a6](https://github.com/HKUDS/nanobot/blob/24c41a6af0736330cd9c67b3c9d1ec9adeefdf5f/nanobot/agent/runner.py)。

核验到的主路径是：

```text
AgentRunner.run
  → 初始化 transcript / compaction
  → _run_core
      → _request_model
      → 判断 should_execute_tools
      → 追加 assistant 调用消息
      → execute_tool_calls
      → 追加对应 tool results
      → 再次请求或结束
```

阅读时特别找 `pending_tool_calls`、`completed_tool_results` 和 checkpoint 的写入点；然后找 malformed call 与预算耗尽分支。它们解释了为什么一条实际循环要比十行 Demo 长。

这条调用路径能支持“小而明确的循环仍需要完整失败语义”的结论；不能据此断言 nanobot 内部就是 Claude Code 的实现。Sonic 吸收消息、执行、记录的分工，不复制所有扩展复杂度。

### 3. DeepSeek Harness：追踪事实从哪里来

固定入口：[architecture.md @ ddefc45](https://github.com/deepseek-ai/deepseek-harness/blob/ddefc45fbc7f8e46dd73185e68295696d1297887/docs/architecture.md)。

该版本将服务和循环组织为插件，并从 Session Log 派生模型消息。对 Sonic 最有价值的问题是：“某次模型到底看到了什么，能否从记录中解释？”把这个约束带回自己的 ContextBuilder，比先实现整套插件框架更直接。

重放需要区分两种目标：重建当时的输入和行为序列可以确定性完成；再次调用远端模型得到完全相同回答通常不能保证。

### 4. 每条外部结论要落到一个自己的实验

| 观察或实现线索 | Sonic 要验证的假设 | 可以运行的实验 |
| --- | --- | --- |
| 自适应工具循环 | 新观察能改变下一动作 | 正常测试失败后读、改、重测 |
| 编辑检查点 | 文件变化可解释且可保留旧内容 | normal 中的 before.bin 断言 |
| 有限上下文 | 去掉旧输出不破坏协议与约束 | context；真实语义摘要另需模型评测 |
| 会话持续工作 | 已完成的 edit 恢复后不重复 | resume；崩溃窗口另需故障注入 |

实验范围窄一些，结论就写窄一些。这是从“学到了一个术语”走到“掌握了一项机制”的分界。

## 十：从编码助手扩展出去

完成编码主链路后，扩展点会自然浮现。

项目指令记录构建方式和代码约定；Skill 把发布或排障流程按需装进上下文；Hook 在明确事件上运行确定程序；MCP 接入外部工具；Subagent 承担独立调查。它们服务的对象不同，不应全部变成“万能插件”。

以“查询 Issue 并修复”为例：MCP 提供读取 Issue 的能力，Skill 可以说明项目的修复流程，主循环仍负责工具结果和后续行动，权限仍由运行时控制。若自动提交 PR，则又增加了一个外部写动作，需独立定义授权和失败语义。

向研究助手扩展时，替换的是工具、上下文来源和验证方式。代码任务看测试和 diff，研究任务看引用能否支持结论。若第二个场景要求重写整个 Loop，说明此前把业务步骤写进了通用控制流。

## 十一：怎样知道真的变好了

### 1. 两种测试回答不同问题

离线假模型测试回答：参数破损会不会执行？拒绝以后有没有改文件？重启以后有没有重复 edit？这类性质应该稳定可复现。

真实模型评测回答：在相同工具、预算和任务集上，模型能否修好未知问题？需要记录模型版本、代码版本、输入、用量和失败类型。Mini Sonic 的 PASS 不能充当这个数字。

### 2. 不只数成功

同样都是没完成，原因可能是模型定位错误、工具输出缺信息、协议错误、权限不足、环境缺依赖或测试设计错误。把这些混成一个成功率，无法指导下一步开发。

先保存失败轨迹，再判断改 Prompt、改工具描述、补上下文，还是修 Harness。比较方案时固定模型和任务，避免换了更强模型却把进步算在自己的上下文算法上。

### 3. 三道毕业题

1. 删掉实验中工具结果消息的 call_id，解释真实协议会在哪一步失去配对；不要只说“可能出错”。
2. 让测试通过后再修改文件，解释为什么原通过记录过期，以及该记录还能用于什么。
3. 让进程在文件写入后、日志落盘前退出，列出恢复时需要检查的事实。当前实验未覆盖这个窗口，应怎样补测？

能够解释这些问题，再看工具目录和架构图，才不会把接口数量误当成工程能力。

## 十二：代码对照与后续维护

| 本课问题 | 可运行实验 | 旧 Sonic 中可读的相关实现 |
| --- | --- | --- |
| 行动循环 | mini_sonic.py：Lab.run | [runner.py](./代码/sonic/src/sonic_agent/core/runner.py) |
| 真实路径边界 | 实验固定单文件，没有通用路径 | [repository.py](./代码/sonic/src/sonic_agent/capabilities/repository.py) |
| 事件与持久化 | Lab.record / reopen | [events.py](./代码/sonic/src/sonic_agent/core/events.py) |
| 上下文选择 | compact_observations | [context.py](./代码/sonic/src/sonic_agent/core/context.py)，尚未接入 Runner |
| 文字结束与验证 | Lab.verification / false_finish | [completion.py](./代码/sonic/src/sonic_agent/core/completion.py) |
| 真正模型调用 | 实验未实现 | [纯文本适配器](./代码/sonic/src/sonic_agent/models/openai_responses.py)，尚无工具流 |

本页随可运行实验与源码一起更新；新增机制须写明问题、失败现象、代码位置和证据。外部来源固定到版本或记录核验日期，无法确认的内部实现明确写作推断。

设计决策和开发进度看[设计说明](./设计说明.md)，面试问答看[面经](./面经.md)。课程实验只服务理解和回归，不建立第二套产品 API；当 Sonic 产品能力完成时，把相关实战入口逐步换成真实 CLI，并保留这些短小的失败复现。
