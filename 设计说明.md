# 设计说明：Sonic 编码 Agent 与通用助手演进方案

> 文档状态：目标设计 v1，2026-09-18。现有 `代码/sonic` 是可复用的教学原型，不代表本文架构已经实现。后续代码可以大幅重构，以本文的产品主线和验收标准为准。

## 1. 一页读懂 Sonic

Sonic 的长期目标是成为一个聪明、强大、可信的个人通用人工智能助手。这个目标不能从“先造一个万能框架”开始，而要从一个高频、边界清楚、结果可验证的真实场景开始。

第一个场景确定为：

> **本地编码 Agent：在用户指定的代码仓库中，理解需求，自主搜索和阅读代码，修改文件，运行命令与测试，根据结果继续迭代，最后用证据交付。**

产品体验参考 Claude Code，代码结构吸收 nanobot 的小核心思想，扩展机制借鉴 DeepSeek Harness 的组合与生命周期设计。Sonic 不追求逐行复刻任何项目，而要形成一套自己能理解、维护和持续演进的实现。

第一阶段做成以后，用户应能在仓库目录运行：

```text
sonic
> 修复用户退出后刷新令牌仍然有效的问题，并补测试
```

Sonic 能够：

1. 理解当前仓库、Git 状态和项目指令；
2. 搜索并读取相关代码；
3. 先复现问题或定位证据；
4. 在权限范围内修改文件；
5. 运行目标测试，根据失败继续工作；
6. 展示工具活动、命令输出和 diff；
7. 接受用户中断、纠正和追加要求；
8. 保存会话，稍后继续或从某处创建分支；
9. 明确报告改动、验证证据和剩余风险。

第一阶段明确不追求：聊天渠道矩阵、长期人格、自动定时任务、复杂多 Agent 网络、插件市场或完全无人值守的高风险操作。它们属于通用助手阶段。

## 2. 产品原则

### 2.1 真实任务优先

每个抽象必须解释它解决了哪个真实编码任务中的问题。没有实际失败案例支撑的“通用能力”，暂不进入 Core。

### 2.2 模型决定路径，系统守住边界

模型可以决定先搜索还是先测试，也可以根据新证据改变计划；系统始终控制可用工具、权限、预算、工作区、取消和完成状态。

### 2.3 小核心，强边缘

Agent Loop 保持可读，核心只理解消息、模型事件、工具调用、用户输入和终止条件。Provider、Tool、Policy、Session Store、UI、Skill 与 MCP 都通过明确接口接入。

### 2.4 证据优于自述

“已经修复”不算完成。文件 diff、命令、退出码、测试结果和未验证项才是交付依据。

### 2.5 用户始终可以介入

运行中允许停止、追加说明和拒绝权限。Sonic 不把自治理解为“用户只能等结果”。

### 2.6 默认保护现有工作

不假设工作树干净，不覆盖用户刚修改的文件，不擅自提交、推送或丢弃变更。所有破坏性动作必须有清楚的范围和结果。

### 2.7 先做单 Agent，再证明需要多 Agent

单 Agent 主链路必须先达到稳定可用。Subagent 是上下文隔离和并行执行手段，不是产品成熟度指标。

## 3. 首个场景的边界

### 3.1 支持的任务

- 回答关于当前代码仓库的问题；
- 定位 Bug、解释根因；
- 实现范围明确的小型功能；
- 重构局部代码并保持行为；
- 补充或修改测试；
- 修复 lint、类型和构建问题；
- 更新与代码直接相关的文档；
- 查看 Git diff、状态和历史；
- 在用户要求时准备提交说明，后续再支持受控提交。

### 3.2 暂不承诺的任务

- 自动发布、部署或操作生产系统；
- 修改仓库之外的任意用户文件；
- 无确认调用付费或高风险外部服务；
- 对任意仓库保证一次成功；
- 自动处理二进制工程、超大单体仓库和所有语言工具链；
- 在没有测试或可观察行为时保证语义完全正确。

### 3.3 MVP 完成定义

只有同时满足以下条件，才称“编码 Agent MVP 可用”：

1. 至少接入一个真实模型 Provider 的流式工具调用；
2. 完整支持 `glob/grep/read/edit/apply_patch/shell`；
3. 在真实 Git 仓库完成“理解—修改—测试—报告”闭环；
4. 用户可以审批、拒绝、取消和中途纠正；
5. 会话重启后可以继续，文件变化不会丢失或被误判；
6. 对脏工作树、并发修改、命令超时和上下文溢出有明确行为；
7. 固定评测集上有可复现结果，不能只有演示视频；
8. 最终回答能区分已验证、未验证和失败。

## 4. 用户体验设计

### 4.1 默认交互

```text
PS repo> sonic

Sonic  ·  workspace: repo  ·  branch: feature/login
mode: ask  ·  model: <configured-model>

> 修复登录超时后页面无限刷新，补一个回归测试

● 正在搜索登录与刷新令牌相关代码
  ├─ grep "refresh" src tests              34 matches
  ├─ read src/auth/session.py:1-220
  └─ read tests/test_session.py:1-180

● 已复现：test_expired_refresh_token 持续重定向
● 准备修改 2 个文件
  └─ 查看 diff / 允许 / 拒绝并说明

● 运行 pytest tests/test_session.py -q
  └─ 18 passed in 2.7s

完成：过期刷新令牌现在会清除会话并返回登录页。
修改：src/auth/session.py、tests/test_session.py
验证：目标测试 18 项通过；未运行全仓测试。
```

用户看到的是可理解的活动，不是内部消息协议。默认不展示隐藏推理；展示动作、输入范围、结果和证据。

### 4.2 首批交互命令

| 命令 | 用途 |
| --- | --- |
| `/help` | 查看命令和使用建议 |
| `/status` | 当前 Session、Run、模型、权限和预算 |
| `/model` | 查看或切换模型 |
| `/mode` | 切换 `plan/ask/edit/auto` |
| `/context` | 查看上下文占用与主要来源 |
| `/diff` | 查看本 Session 产生的文件变化 |
| `/undo` | 恢复最近文件 checkpoint |
| `/compact` | 主动压缩当前会话 |
| `/resume` | 选择历史 Session 继续 |
| `/fork` | 从当前历史创建独立 Session |
| `/clear` | 开始新的 Session，不修改工作区 |

### 4.3 用户中途输入

用户在 Agent 工作时发送新消息，默认分两种：

- **立即停止**：明确输入停止或按取消键，当前可取消动作终止；
- **转向消息**：消息进入当前 Run 的高优先级输入队列，在当前原子工具调用结束后交给模型。

转向消息必须出现在下一次模型请求里，并记录它在哪个工具调用之后生效。

## 5. 总体架构

```text
┌──────────────────────── Experience Plane ────────────────────────┐
│ CLI/TUI  Commands  Streaming View  Approval UI  Diff/Checkpoint │
└──────────────────────────────┬───────────────────────────────────┘
                               │ commands + events
┌──────────────────────── Control Plane ───────────────────────────┐
│ Application  SessionService  AgentLoop  RunController            │
│ InputQueue   Cancellation    Completion/Verifier                 │
└──────────────┬───────────────────────┬───────────────────────────┘
               │                       │
┌──────────────▼──────── Context ──────▼───────────────────────────┐
│ ContextEngine  ProjectInstructions  Compactor  TokenBudget       │
│ ConversationStore  Artifact references  Optional memory later    │
└──────────────┬───────────────────────────────────────────────────┘
               │ model request / stream
┌──────────────▼──────── Model Plane ──────────────────────────────┐
│ ModelGateway  Provider adapters  Retry/usage/capability mapping   │
└──────────────┬───────────────────────────────────────────────────┘
               │ tool calls
┌──────────────▼──────── Capability Plane ─────────────────────────┐
│ ToolRegistry → Argument validation → Policy → Approval           │
│ ToolRuntime  → Workspace/Sandbox → File/Shell/Git tools          │
└──────────────┬───────────────────────────────────────────────────┘
               │ normalized events
┌──────────────▼──────── Data Plane ───────────────────────────────┐
│ Session DB  Event/Trace Store  Checkpoints  Artifacts  Eval data │
└──────────────────────────────────────────────────────────────────┘
```

“Plane”只是帮助理解职责，不意味着每一层都要独立部署。MVP 是一个本地 Python 进程和一个持久化目录。

## 6. 核心运行链路

### 6.1 一次 Run 的算法

```python
async def run(session_id: str, user_message: str) -> RunResult:
    run = await sessions.start_run(session_id, user_message)

    while not run.terminal:
        await controller.apply_pending_user_input(run)
        controller.check_cancelled_and_budget(run)

        context = await context_engine.build(run)
        stream = model_gateway.stream(context, tools.visible_specs(run))

        async for event in stream:
            await events.append(run.id, event)
            await ui.publish(event)

            if event.is_complete_tool_call:
                result = await tool_runtime.execute(run, event.tool_call)
                await sessions.append_observation(run, result)

        if stream.has_final_text:
            verdict = await verifier.evaluate(run, stream.final_text)
            return await sessions.finish(run, verdict)
```

真实实现需要处理多个并行 Tool Call、流中断和审批暂停，但主链路应始终保持能在一个文件中读懂。

### 6.2 Run 状态机

```text
created
  → building_context
  → waiting_model
  → handling_response
      ├─ waiting_approval → running_tool → handling_response
      ├─ waiting_user
      ├─ verifying → completed
      └─ waiting_model（继续下一轮）

任意非终态：→ cancelling → cancelled
预算耗尽：   → stopped
内部故障：   → failed
未知副作用： → needs_reconciliation
```

终态含义必须稳定：

| 终态 | 含义 |
| --- | --- |
| `completed` | 已满足完成契约，并有相应证据 |
| `partial` | 完成了一部分，剩余工作明确 |
| `cancelled` | 用户或系统停止；不暗示效果已回滚 |
| `stopped` | 达到时间、token、费用或调用次数上限 |
| `failed` | 明确失败，且知道失败发生在哪里 |
| `needs_reconciliation` | 外部动作状态无法确认，需人工或查询目标系统 |

### 6.3 Tool Call 状态机

```text
proposed → validated → allowed ──────────────→ started → succeeded
                    └→ approval_required → approved ┘       │
                                        └→ denied           ├→ failed
proposed → invalid                                        └→ unknown
```

文件读写通常能得到明确结果；网络 API 或进程崩溃时可能出现 `unknown`。遇到未知副作用不能自动重试。

## 7. 核心数据契约

以下是概念接口，字段可以在实现中调整，但所有权不能模糊。

### 7.1 Session 与 Run

```python
@dataclass(frozen=True)
class Session:
    id: str
    workspace_id: str
    parent_session_id: str | None
    created_at: datetime
    title: str | None

@dataclass
class Run:
    id: str
    session_id: str
    status: RunStatus
    user_goal: str
    started_at: datetime
    permission_mode: str
    budget: "RunBudget"
    workspace_snapshot: "WorkspaceSnapshot"
```

Session 是历史容器；Run 是一次执行。一个 Session 可以有多个 Run。切换模型不创建新 Session；Fork 会创建新的 Session ID。

### 7.2 Model Request 与事件

```python
@dataclass(frozen=True)
class ModelRequest:
    messages: list[Message]
    tools: list[ToolSpec]
    model: str
    max_output_tokens: int | None
    metadata: dict[str, str]

class ModelEvent: ...
# TextDelta / ToolCallDelta / ToolCallCompleted /
# Usage / Completed / Failed
```

Provider 必须声明能力，例如工具调用、流式、图像输入、Prompt Cache 和最大上下文。Core 根据能力协商，不在业务流程里判断供应商名称。

### 7.3 Tool Spec、Context 与 Result

```python
@dataclass(frozen=True)
class ToolSpec:
    name: str
    description: str
    input_schema: dict
    risk: str                    # read / write / execute / external
    version: str

@dataclass(frozen=True)
class ToolContext:
    session_id: str
    run_id: str
    workspace: "Workspace"
    approval: "ApprovalGrant | None"
    cancellation: "CancellationToken"

@dataclass(frozen=True)
class ToolResult:
    ok: bool
    content: str
    error_code: str | None
    retryable: bool
    metadata: dict
    artifact_ids: tuple[str, ...] = ()
```

Tool 不直接接触全局 Session 对象，也不能自行扩大工作区。长输出通过 Artifact 引用，不无限写入模型上下文。

### 7.4 Event Envelope

```python
@dataclass(frozen=True)
class Event:
    id: str
    sequence: int
    session_id: str
    run_id: str | None
    type: str
    timestamp: datetime
    payload: dict
    schema_version: int
```

每个 Run 的 `sequence` 单调递增。事件先持久化，再驱动 UI 和投影；正文和敏感内容按类型最小化存储。

## 8. 组件详细设计

### 8.1 Application / Composition Root

Application 是唯一负责组装和关闭组件的地方：读取配置、创建 Store、Provider、ToolRegistry、Policy、AgentLoop 和 UI。

它不实现 Agent 逻辑。MCP 连接、后台进程和数据库连接的生命周期由 Application 管理，AgentLoop 只消费已准备好的接口。

### 8.2 SessionService

职责：

- 创建、命名、列出、恢复和分叉 Session；
- 把用户消息、模型消息和工具观察写入历史；
- 保证同一 Session 的活动 Run 不被无意并发推进；
- 管理摘要、checkpoint 和 artifact 关联；
- 在启动时识别上次异常中断的 Run。

Session 文件不放在目标仓库中，避免污染项目和被 Agent 当作普通文件修改。仓库内只读取明确的项目指令文件。

### 8.3 AgentLoop

AgentLoop 只负责：

1. 接收一次 Run 的目标；
2. 请求 ContextEngine 生成模型输入；
3. 消费规范化 ModelEvent；
4. 把完整 Tool Call 交给 ToolRuntime；
5. 把 Observation 放回会话；
6. 处理用户转向、取消、预算和终止；
7. 调用 Verifier 形成最终状态。

不负责：解析具体 Provider 响应、实现文件系统操作、拼接 TUI、直接读写 SQLite。

### 8.4 ModelGateway

ModelGateway 提供统一 Provider 接口，并集中处理：

- 模型选择和能力发现；
- 流式协议规范化；
- token 使用量、时延和请求 ID；
- 可安全重试的传输故障；
- 限流退避；
- 凭据从环境或安全存储注入；
- Provider 错误脱敏。

重试只覆盖“可以确认请求没有产生不可接受重复效果”的模型请求；Tool 绝不因为模型重试而重复执行。

### 8.5 ToolRegistry 与 ToolRuntime

Registry 管理 ToolSpec 和执行器，Runtime 负责统一流水线：

```text
lookup → validate → normalize → policy → approval
       → emit started → execute → normalize result → emit completed
```

工具通过显式注册进入某个 Profile。被完全禁用的工具不应出现在模型上下文中；只禁用部分参数的工具仍可见，但每次调用都由 Policy 判断。

### 8.6 Workspace

Workspace 是能力对象，不只是路径字符串。它提供：

- 固定根目录与允许的附加目录；
- 路径规范化和真实路径校验；
- 初始 Git 快照与当前变化；
- 安全的文件读写和原子替换；
- checkpoint 创建与恢复；
- 可选的隔离执行后端。

模型工具不能传入新的根目录。切换项目由用户界面或可信 Application 完成。

### 8.7 Policy 与 Approval

Policy 输入：主体、权限模式、ToolSpec、规范化参数、Workspace 和已有 Grant。输出：

```python
Decision = Allow() | Ask(prompt, grant_scope) | Deny(code, reason)
```

规则优先级为 `deny > ask > allow`。ApprovalGrant 至少绑定：

- Tool 名称与版本；
- 规范化参数或清楚的参数模式；
- Workspace ID；
- Session/Run 范围；
- 过期时间；
- 批准来源。

用户拒绝时，拒绝原因作为 Observation 返回模型，使其可以换一种安全方法，而不是整个进程直接崩溃。

### 8.8 Shell Runtime

MVP 支持前台命令和有限的长进程会话：

- 明确 `cwd`，默认 Workspace 根；
- 超时和取消；
- stdout/stderr 增量事件；
- 输出截断与 artifact；
- 退出码和持续时间；
- 不使用交互式登录 Shell 注入未知配置；
- Windows、Linux 的命令构造分别测试，不用字符串拼接模拟 argv。

应用级路径校验不是 OS 沙箱。`auto` 模式和未来无人值守任务必须优先在容器、worktree 或平台沙箱中运行。

### 8.9 CheckpointManager

文件写入前记录受影响文件的内容哈希和快照，写入后记录新哈希。Checkpoint 支持：

- 查看某次工具调用的 diff；
- 按工具调用撤销；
- 检测文件已被用户再次修改并拒绝盲目恢复；
- Session 恢复后继续使用。

首版只覆盖普通文件变化，不宣称可以撤销 Shell 的任意副作用。

### 8.10 Verifier

Verifier 不判断“代码是否绝对正确”，而是把可验证证据转成明确结论：

- 用户要求的文件或行为是否处理；
- 是否仍有失败命令；
- 目标测试、相关测试和静态检查结果；
- diff 是否为空、是否超出任务范围；
- 模型是否声称执行了实际未执行的验证；
- 是否存在未批准或状态未知的动作。

Verifier 输出 `completed/partial/failed` 和证据列表。模型负责生成友好说明，系统保留最终状态决定权。

## 9. 上下文系统

### 9.1 Context Sources

| 来源 | 生命周期 | 默认策略 |
| --- | --- | --- |
| Sonic 系统规则 | 版本级 | 始终保留 |
| 项目指令 | Workspace 级 | 启动和变化时读取 |
| 用户目标/限制 | Run 级 | 始终保留 |
| Session 摘要 | Session 级 | 压缩后保留 |
| 最近消息 | Turn 级 | 保留合法后缀 |
| 工具定义 | Profile 级 | 只发送可见工具；后续支持按需加载 |
| 文件与搜索结果 | Observation | 按相关性、时效和预算裁剪 |
| 命令输出 | Observation/Artifact | 上下文中只留关键片段 |

### 9.2 ContextItem

每个条目至少包含：

```text
id / kind / source / content-or-artifact-ref
priority / token_estimate / created_at
workspace_revision / can_trim / trust_level
```

`trust_level` 用于提示模型区分用户指令、项目规则和低信任仓库内容，但真正权限仍由执行层强制。

### 9.3 预算算法

```text
可用输入预算
= 模型上下文上限
- 预留输出
- 安全余量
- Tool Schema
- 不可丢层
```

剩余预算依次分配给任务状态、近期消息和按需证据。单个工具结果超过上限时保存 Artifact，仅返回摘要、头尾和定位信息。

### 9.4 自动压缩触发

满足任一条件触发：

- 下一轮估算超过上下文软上限；
- 工具正文占比超过阈值；
- Session 已完成多个阶段，旧细节不再需要；
- 用户主动执行 `/compact`。

压缩输出使用结构化模板：目标、限制、已确认事实、改动、验证、失败尝试、未完成事项、重要引用。生成后进行约束保留检查；压缩失败时停止扩张并提示用户，不能无限压缩循环。

## 10. 工具与权限清单

### 10.1 MVP 工具

| 工具 | 默认风险 | 默认权限 |
| --- | --- | --- |
| `fs.glob` | read | 工作区内自动 |
| `fs.grep` | read | 工作区内自动 |
| `fs.read` | read | 工作区内自动，敏感文件拒绝/询问 |
| `fs.edit` | write | `ask` 模式询问，`edit` 模式自动 |
| `fs.apply_patch` | write | 同上，展示目标文件 |
| `shell.exec` | execute | 只读白名单可自动，其余询问 |
| `shell.poll/write/stop` | execute | 继承启动进程的 Grant |
| `git.status/diff/log` | read | 自动 |
| `checkpoint.restore` | write | 展示影响后询问 |

`git commit/push`、网络请求、MCP 和外部系统写操作不进入第一版默认工具集。

### 10.2 命令风险分级

不能只按命令开头判断风险。规范化后至少考虑：

- 可执行文件；
- 完整 argv 和重定向；
- 工作目录；
- 环境变量覆盖；
- 是否启动脚本、包管理器或子 Shell；
- 是否可能联网、安装依赖或修改仓库外文件。

复杂命令无法可靠分类时使用 `ask` 或进入隔离环境，不用脆弱的字符串规则假装安全。

## 11. 持久化与目录

### 11.1 用户数据目录

概念布局：

```text
~/.sonic/
├── config.toml
├── sessions/
│   └── <workspace-id>/
│       ├── <session-id>.jsonl
│       └── summaries/
├── state.db
├── checkpoints/
├── artifacts/
├── logs/
└── skills/                 # 后续阶段
```

Workspace ID 由规范化路径和随机身份组合生成，避免目录移动、复制或同名项目造成会话串线。凭据不写入 Session 或仓库。

### 11.2 存储选择

MVP 使用 SQLite 保存结构化元数据和事件索引，Artifact/Checkpoint 使用文件存储。原因：本地部署简单、事务明确、可查询。JSONL 可作为导出和调试格式，不作为所有并发状态的唯一数据库。

### 11.3 恢复流程

启动恢复 Session 时：

1. 读取最后一个 Run 的终态；
2. 若异常中断，检查最后一个模型和工具事件；
3. 校验 Workspace 路径、Git HEAD 和相关文件哈希；
4. 恢复可确认的消息和 Observation；
5. 对状态未知的外部动作停止并提示对账；
6. 新 Run 继续，而不是篡改历史 Run。

## 12. 项目代码结构

目标目录如下；随着重构逐步迁移，不要求一次性生成空目录：

```text
代码/sonic/
├── pyproject.toml
├── src/sonic/
│   ├── app.py                 # composition root
│   ├── cli/                   # CLI/TUI、命令、渲染
│   ├── agent/
│   │   ├── loop.py            # 唯一主循环
│   │   ├── controller.py      # 取消、预算、用户转向
│   │   ├── state.py
│   │   └── verifier.py
│   ├── context/
│   │   ├── builder.py
│   │   ├── budget.py
│   │   └── compaction.py
│   ├── models/
│   │   ├── base.py
│   │   ├── events.py
│   │   └── providers/
│   ├── tools/
│   │   ├── registry.py
│   │   ├── runtime.py
│   │   ├── filesystem.py
│   │   ├── shell.py
│   │   └── git.py
│   ├── security/
│   │   ├── policy.py
│   │   ├── approvals.py
│   │   └── workspace.py
│   ├── sessions/
│   │   ├── service.py
│   │   ├── store.py
│   │   └── checkpoints.py
│   ├── observability/
│   │   ├── events.py
│   │   └── tracing.py
│   └── extensions/            # 后续：skills/hooks/mcp/subagents
└── tests/
    ├── unit/
    ├── integration/
    ├── scenarios/
    └── fixtures/
```

目录反映依赖方向：`agent` 依赖抽象接口，不依赖具体 CLI 或某家 Provider；Tools 通过 ToolContext 获取能力，不读取任意全局变量。

## 13. 现有代码怎样处理

当前 `代码/sonic` 版本约为教学内核 v0.5，已经有：

- 同步 FakeProvider Agent Loop；
- 事件存储和状态投影原型；
- 模型与工具调用次数限制；
- OpenAI Responses 纯文本 Provider 基线；
- 固定根目录的只读 `list/read/search`；
- Tool Registry、错误返回和少量策略原型；
- 相关单元测试。

它没有形成可用编码 Agent：没有真实 Provider 工具闭环、流式 CLI、写工具、Shell、审批、checkpoint、Session 恢复、压缩和代码任务 Verifier。

处理策略不是“原地保住所有 API”，而是逐项判断：

| 现有模块 | 决定 |
| --- | --- |
| repository 路径与资源限制 | 保留思路和安全测试，迁入 Workspace + filesystem tools |
| Provider 元数据与错误脱敏 | 保留测试目标，重做流式接口 |
| 事件 envelope、revision | 保留概念，按新 Session/Run 结构调整 |
| 同步 AgentRunner | 用 async 流式 AgentLoop 替换 |
| ExactTextCompletion | 删除产品依赖，仅可留作测试 fixture |
| 内存 EffectJournal | 不作为产品保证；等外部写工具出现时重做持久 effect |
| 当前包名 `sonic_agent` | 重构阶段迁移为 `sonic`，提供短期兼容或一次性切换 |

历史代码可以被删除，只要新的场景测试覆盖同等重要的边界。Git 历史就是备份，不保留两套主循环。

## 14. 开发路线与门禁

### M0：重置产品骨架

交付：

- 新包结构和 async 事件模型；
- 最小 CLI；
- FakeProvider 流式工具循环；
- Session/Run 基础存储；
- 旧测试中值得保留的安全边界迁移。

通过标准：一个离线脚本可以流式展示 `read → tool result → final`，重启后能查看历史 Run。

### M1：真实模型只读 Agent

交付：

- 一个真实 Provider 的文本和工具流；
- `glob/grep/read/git status/diff/log`；
- ContextBuilder 和项目指令；
- token、时延、错误和取消。

通过标准：对至少 20 个固定仓库问题给出带有效文件引用的答案；越界读取为硬失败。

### M2：可控编辑

交付：

- `edit/apply_patch`；
- 文件版本检查、原子写入和 checkpoint；
- `/diff`、`/undo`；
- `plan/ask/edit` 权限模式。

通过标准：能完成小型多文件改动；不会覆盖模拟的用户并发修改；撤销有自动化测试。

### M3：命令与验证闭环

交付：

- `shell.exec`、超时、增量输出和取消；
- 长进程最小支持；
- Verification Ladder；
- 真实任务最终报告。

通过标准：在固定 Bug/功能任务集上完成“复现—修改—测试—报告”；测试失败不能标记完成。

### M4：长会话可用性

交付：

- Session resume/fork；
- 自动压缩和 `/context`；
- 用户转向消息；
- 崩溃恢复和异常 Run 检查。

通过标准：长任务跨进程继续，用户限制经过压缩仍能保留，未知动作不会盲目重试。

### M5：扩展层

按真实需求依次加入：Hooks、Skills、MCP、Subagents。每项必须有独立接口、生命周期、权限和场景评测。

### M6：向通用个人助手扩展

优先选择第二个能验证通用性的垂直场景，例如“带来源的技术研究助手”。它复用 Session、Context、Tool、Policy 和 Event，只增加新的 Tool/Profile/Verifier。若必须重写 AgentLoop，说明 Core 抽象仍不够通用。

之后才考虑长期 Memory、多设备入口、定时任务和跨应用操作。

## 15. 测试与评测体系

### 15.1 四层测试

| 层级 | 目的 | 示例 |
| --- | --- | --- |
| Unit | 单个契约和边界 | 路径解析、预算、Policy 优先级 |
| Contract | 所有实现遵守同一接口 | Provider 流事件、ToolResult 错误码 |
| Integration | 多组件真实组合 | 模型工具回路、checkpoint + diff |
| Scenario/Eval | 用户任务是否完成 | 修 Bug、补功能、解释代码 |

测试不能依赖真实付费模型才能运行。CI 使用 FakeProvider 和录制/合成事件；单独的受控任务执行真实模型评测。

### 15.2 首批场景集

至少覆盖：

- 明确单文件 Bug；
- 需要跨文件追踪的 Bug；
- 需要先复现的失败；
- 测试缺失；
- 用户描述错误或证据不足；
- 工作树已有用户修改；
- 仓库含提示注入文本；
- 命令超时或输出爆量；
- 修改后目标测试通过但相关测试失败；
- 无法安全完成，需要明确停止。

### 15.3 核心指标

- 任务成功率，按场景类型分桶；
- 错误完成率：声称完成但证据不支持；
- 非任务范围文件修改率；
- 未授权动作次数；
- 首次有效动作时间和总时延；
- 输入/输出 token、缓存命中和估算费用；
- 用户中途纠正后恢复正确率；
- 崩溃恢复与 checkpoint 正确率。

安全违规和错误完成是硬门禁，不能被平均成功率掩盖。

### 15.4 Golden Trace

复杂失败保存可脱敏的 Golden Trace：模型事件、工具调用、结果摘要、工作区版本和最终判断。它用于回归 Harness 行为，不要求模型每次生成完全相同文字。

## 16. 可观测性与故障处理

每次 Run 至少可查询：

- 使用的 Sonic、模型、Tool Schema 和项目指令版本；
- 每轮上下文 token 构成；
- 模型时延、请求 ID、用量和失败类型；
- 工具输入摘要、权限决定、时延、退出码和输出截断；
- 文件变化与 checkpoint；
- 最终 Verifier 证据。

日志、事件和模型上下文是三个不同视图：

- 日志服务开发者调试；
- 事件服务 UI、恢复和审计；
- 上下文只包含模型完成当前任务所需的内容。

任何错误都要落入稳定类别：`invalid_input`、`permission_denied`、`not_found`、`conflict`、`timeout`、`cancelled`、`provider_rate_limit`、`provider_unavailable`、`internal` 等。模型看到安全信息，开发日志保留详细诊断。

## 17. 扩展为通用助手时保持什么不变

编码场景之外仍应复用：

- Session/Run/Turn/Event 模型；
- AgentLoop 和用户转向机制；
- ContextItem、预算和压缩；
- Provider 流事件；
- ToolSpec、Policy、Approval 和 ToolResult；
- Artifact 与长输出；
- Verifier 接口；
- CLI/API/其他 Channel 的事件消费方式。

需要扩展的是 Profile：可用工具、系统规则、上下文来源和完成契约。例如研究助手增加 Web/Search 与引用 Verifier；个人事务助手增加日历和邮件 Tool，并采用更严格的外部副作用确认。

长期 Memory 不等于把所有历史向量化。它至少区分：

- 用户明确要求记住的偏好；
- 可纠正、可删除的个人事实；
- 某个项目独有的知识；
- 只属于当前 Session 的临时状态。

在首个编码场景稳定前，不实现自动长期记忆。

## 18. 如何持续跟进外部技术

外部参考按三个问题研究：

1. 它提供了什么可观察行为？
2. 它解决了 Sonic 已经遇到的哪个问题？
3. 我们怎样用最小实验确认收益和代价？

研究资料可以包括官方文档、开源源码、发布包、行为实验、反编译/重构版本和社区分析。每条重要结论记录项目版本、观察日期和证据等级。来源较弱的材料用于提出假设，再由可复现实验或多份材料交叉确认。

采用新机制前写清：

```text
当前失败样本
→ 候选设计
→ 最小实现
→ 对照指标
→ 新增风险与撤回办法
```

不因为 Claude Code、DeepSeek Harness 或 nanobot 增加某功能就自动跟进。Sonic 学习它们的解决问题方式，不追逐功能数量。

## 19. 仓库与文档约束

仓库长期只保留四份顶层 Markdown：

| 文件 | 唯一职责 |
| --- | --- |
| `README.md` | 导航、快速开始、当前版本摘要 |
| `学习文档.md` | Harness 与编码 Agent 的体系化知识和动手示例 |
| `设计说明.md` | Sonic 产品、架构、状态、路线和验收标准 |
| `面经.md` | 相对独立的面试题与每周外部更新 |

不再为路线图、ADR、周报或模块说明不断新增 Markdown。重要决策记录在本页“关键决策”中；代码附近只保留必要 docstring 和注释。

文档规则：

- 先讲目的和结论，再讲实现细节；
- 首次出现的术语用通俗语言解释；
- “已实现”必须链接代码或测试；
- “目标设计”不能写成当前事实；
- 示例命令必须可以复制，或明确标注伪代码；
- 学习文档讲共性，设计说明讲 Sonic，不大段重复；
- 代码行为改变时同一提交更新相关文档。

Git 规则：

- GitHub `main` 是唯一基准；修改前先 fetch 并确认工作树；
- 功能通过 `codex/...` 分支和 PR 合入，不强推远端；
- 不提交密钥、真实私密会话、缓存、生成 ZIP 或重复源码快照；
- 发布下载使用 Git tag/Release 或指定 commit archive；
- 每个 PR 说明用户行为、测试、文档影响和剩余风险。

## 20. 关键决策记录

| 决策 | 当前选择 | 主要原因 | 复审条件 |
| --- | --- | --- | --- |
| 第一场景 | 本地编码 Agent | 高频、结果可验证、能压测 Harness 全链路 | MVP 完成后选择第二场景 |
| 实现语言 | Python 3.11+ | 个人项目迭代快、生态和可读性好 | TUI/并发/分发成为明确瓶颈 |
| 并发模型 | asyncio + 结构化取消 | 流式模型、进程输出和用户输入需要并发 | 性能数据证明需要多进程组件 |
| Core 形态 | 小型显式 AgentLoop | 易理解、易测试、避免过早插件化 | 多个 Profile 反复修改 Core |
| 持久化 | SQLite + 文件 Artifact | 本地简单、事务清楚、可查询 | 多机协作或远程服务成为需求 |
| 文件修改 | 专用 Edit/Patch + Checkpoint | diff 清楚、冲突可检测、可撤销 | 大型二进制或 IDE AST 编辑需求出现 |
| Shell | 保留但受 Policy 与 Sandbox 约束 | 构建和测试无法全部结构化 | 不取消，只改隔离后端 |
| 多 Agent | MVP 后按评测引入 | 先解决主循环，再承担协调成本 | 独立任务的并行收益可量化 |
| 长期 Memory | 编码 MVP 后 | 先避免错误记忆和权限复杂度 | 用户跨 Session 需求已被验证 |
| 兼容旧 API | 不保证 | 旧代码是原型，不让兼容性拖住正确架构 | 发布第一个稳定版本后开始版本承诺 |

## 21. 下一步实施顺序

接下来不继续在旧同步 Runner 上堆功能，按下面顺序进入代码重构：

1. 建立新的 `sonic` 包、async ModelEvent 和 AgentLoop；
2. 用 FakeProvider 打通流式文字、工具调用和事件输出；
3. 建立 Session/Run SQLite 存储和最小 CLI；
4. 迁移现有只读路径边界测试，完成新的 `glob/grep/read`；
5. 接入第一个真实 Provider 的工具流；
6. 再进入 Edit、Checkpoint、Approval 和 Shell。

每一步都必须产生一个用户可运行的纵向切片，而不是只创建接口和空目录。第一条真正重要的里程碑不是“架构搭好了”，而是 Sonic 能在一个真实仓库里自主找到答案，并给出可核对的代码引用。
