# 设计说明：sonic 通用 AI 助手

> 2026-09-16。本文同时说明产品目标、当前代码、关键取舍、演进门禁和仓库约束。**设计目标不等于已实现能力**。旧 TraceForge 教学设计保留在 Git 历史；现行产品名统一为 sonic。

## 先看结论

sonic 的终点是一个聪明、强大且可信的通用 AI 助手：理解用户目标，选择回答、检索、计划、执行或澄清，在授权范围内行动，用证据交代结果。第一阶段先把**受限仓库问题助手**做成真实可用的垂直场景，然后用非代码任务检验同一个内核是否真的通用。

当前仓库还只是 Python 标准库教学内核。它能跑 FakeProvider 演示和 29 项测试；**不能**接真实模型独立修复仓库，也没有生产级沙箱、审批恢复或跨进程副作用对账。

阅读本页可按：[产品目标](#产品目标与非目标) → [一次任务](#一次任务怎样通过每一道门) → [当前能力](#能力状态与代码证据) → [安全边界](#安全与隐私边界) → [演进门禁](#演进路线与验收门禁) → [仓库约束](#仓库维护与-git-约束)。通用 Harness 原理看[学习文档](./学习文档.md)，面试题看[面经](./面经.md)。

## 产品目标与非目标

用户说：“找出金额计算错误，只能改 `src/pricing.py`，不能联网，修完跑测试。”一个普通模型循环可能给出补丁并声称“修好了”；sonic 还必须保存原始限制、校验工具与工作区、记录动作、核对 diff 和测试，证据不足就说未验证。以后用户要求“比较三份公开资料并给出处”时，不需要补丁和测试命令，但仍需要来源、预算、状态与验收。

“聪明”不只是回答流畅，要能选合适的行动或主动澄清；“强大”不只是工具多，要能替换模型、任务能力和入口；“可信”要求不越权、不盲重试、不虚假完成。它们分别用任务成功、错误完成、未授权写入、重复副作用、恢复后不确定状态、成本与时延来观察。

首版不搭任意插件树或多 Agent 自治网络，不默认接入全部 MCP 工具，不自动发布代码或对外发消息，不保存未经许可的个人记忆。新抽象须由第二个真实任务或可重复的失败样例证明，而不是因为它流行。

## 一次任务怎样通过每一道门

```text
CLI / 以后更多入口
  → 身份、会话引用、原始任务与授权范围
  → 判断：直接回答 / 检索 / 规划 / 执行 / 先澄清
  → Task Profile：任务阶段、证据源、可用工具、完成条件
  → sonic Core：RunState、事件、Context、预算、取消与终态
      ↔ Provider：模型只给回答或工具提案
      → ToolRuntime：输入校验 → 策略/审批 → 隔离执行 → 效果对账
  → Verifier：按任务核验结果
  → 报告：完成 / 失败 / 未验证 / 需要用户处理
```

一个会话可包含多个 Run；每个 Run 独立结算预算、审批和结果。RunState 是运行事实的可重建视图，不是对话摘要；长期 Memory 是跨任务的获许可候选知识，不该混进运行状态。当前代码只有独立 Run 的教学子集，没有正式会话或长期记忆。

Core 只掌管通用的请求、模型提案、受控执行、观察、预算、事件和终态。RepoFix 的“定位→补丁→测试”属于 Task Profile，完成条件属于该任务的 Verifier；资料研究应换 Profile 而不改 Core 的控制流。目标 Profile 至少描述任务输入、阶段、上下文来源、工具清单、权限收紧范围、完成契约和评测集。**当前代码尚无正式 Profile 接口**。

参考 [Claude Code 的公开工作机制](https://code.claude.com/docs/en/how-claude-code-works)、[DeepSeek Harness 的架构](https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/architecture.md)与 [nanobot 的概念设计](https://github.com/HKUDS/nanobot/blob/main/docs/concepts.md)时，只借鉴可核验的公开思路；外部项目的具体行为和来源在[学习文档](./学习文档.md#下篇工程专题与公开项目对照)讨论，不推断私有内核。

## 能力状态与代码证据

这里的“已实现”仅指教学范围。代码改变时，须在同一 PR 更新证据与限制。

| 能力 | 状态 | 证据 | 不能据此声称 |
| --- | --- | --- | --- |
| FakeProvider 与模型/工具循环 | 已实现 | [Runner](./代码/sonic/src/sonic_agent/core/runner.py)、[模型边界](./代码/sonic/src/sonic_agent/models/model.py)、[循环测试](./代码/sonic/tests/test_runner_projection.py) | 已接真实模型或能完成真实 RepoFix。 |
| JSONL/SQLite 本地事件、revision 冲突与 RunState 投影 | 已实现 | [Events](./代码/sonic/src/sonic_agent/core/events.py)、[State](./代码/sonic/src/sonic_agent/core/state.py)、[核心测试](./代码/sonic/tests/test_core_contract.py) | SQLite 事务覆盖外部工具，或已能安全续跑未完成动作。 |
| 显式完成契约 | 已实现 | [Completion](./代码/sonic/src/sonic_agent/core/completion.py)、[契约测试](./代码/sonic/tests/test_core_contract.py) | 固定文本验收等于补丁/测试验收。 |
| 模型与工具次数、deadline、动作间取消、精确重复观察停止 | 已实现 | [Runner](./代码/sonic/src/sonic_agent/core/runner.py)、[边界测试](./代码/sonic/tests/test_core_contract.py) | 已有 token/费用账本、进程组取消或语义震荡检测。 |
| 简单 Tool Registry、路径段拒绝与同进程结果复用 | 已实现 | [Tools](./代码/sonic/src/sonic_agent/capabilities/tools.py)、[工具测试](./代码/sonic/tests/test_tools.py) | 路径策略是沙箱，或 EffectJournal 崩溃后仍能去重。 |
| ContextBuilder 教学示例 | 已实现 | [Context](./代码/sonic/src/sonic_agent/core/context.py)、[上下文测试](./代码/sonic/tests/test_model_context.py) | 已接入 Runner 或必保留项绝不超预算。 |
| 真实模型、RepoFix Profile/Verifier、隔离与授权写入 | 设计中 | 本页[演进门禁](#演进路线与验收门禁) | 已有真实任务成功率。 |
| 跨进程效果对账、资料研究、多入口、许可记忆 | 设计中 | 本页[安全设计](#安全与隐私边界) | 已有通用助手或个人事务能力。 |
| 真实模型成功率、成本/时延改进、恢复正确率 | 待验证 | 尚无相应实验报告 | 可写成成果或简历数字。 |

## 代码怎么运行与阅读

进入 `代码/sonic`，使用 Python 3.10+：

```powershell
$env:PYTHONPATH = "src"
python examples/demo.py
python -m unittest discover -s tests -v
```

演示由 FakeProvider 先调用 `add`，再返回固定文本；`ExactTextCompletion` 通过才显示 `completed`。当前基线是 29 项测试通过，不需要 API Key。读源码先看 `core/runner.py`，再读 `core/events.py` 与 `core/state.py`，然后看 `capabilities/tools.py`、`core/completion.py` 和失败边界测试。当前 0.3.0 是教学内核版本，不是 sonic 产品可用性声明。

## 安全与隐私边界

模型输出是**提案**，不是执行许可。工具输入的形状校验、业务条件、用户授权、Task Profile 的收紧范围和系统能力要取交集；危险写入的审批绑定具体参数、资源、代码版本及有效期。真实路径、符号链接、命令和网络边界由隔离层限制。当前 `DenyPathPolicy` 只按字符串路径段拒绝，不能当沙箱。

外部动作采用“先保存稳定身份与意图 → 执行 → 保存 receipt → 不明时对账”的目标协议。若写入已发生而 receipt 未记录，重启先检查实际资源，必要时标记待对账；不能盲重试，也不能承诺“恰好一次”。当前 `EffectJournal` 仅是同进程内存字典；SQLite 的本地事件事务并不覆盖外部副作用。

模型说 Finish 只是申请验收。RepoFix 应检查允许路径、diff、目标与相关回归测试；资料研究应检查来源、覆盖与冲突；外部事务应检查 receipt。证据不可用时报告 `UNVERIFIED`。取消只能阻止未发生的动作，不能把已经发生的外部效果说成撤销。

网页、仓库文件与工具输出是较低信任内容，不能借“忽略用户限制”等文字修改授权。公开仓库不提交 API Key、真实会话、个人记忆或凭据；未来记忆须有来源、用户许可、纠错与删除办法。当前代码不应在敏感真实仓库上自动写入。

## 演进路线与验收门禁

| 阶段 | 要交付什么 | 过关证据 |
| --- | --- | --- |
| 0 · 教学基线与四文档 | 保留教程、48 题、29 项测试；能力说法与代码一致 | 四份 Markdown、入口可达、测试全绿、旧资料可由 Git 追溯。 |
| 1A · 只读仓库诊断 | 真实模型、固定仓库版本、受限读取、可复现定位报告 | 至少 20 个固定任务，含歧义/无解/越界负例；公开失败分桶与简单基线，不修改文件。 |
| 1B · 授权修复 | 隔离工作区、允许路径/命令、持久化效果对账、补丁/测试 Verifier | 故障注入覆盖意图、写入、receipt 与终态边界；无未授权写入、错误完成或不明结果盲重试。 |
| 2 · 非代码场景 | 带来源的只读资料研究 Profile | 两种任务共用同一 Core，固定评测；若 Runner 写死 RepoFix 分支，先修接口。 |
| 3 · 通用助手扩展 | 会话、多入口、经许可的记忆与有审批/receipt 的跨应用动作 | 用户授权、停用/删除机制、成本与安全评测齐全；多 Agent 等由数据证明需求。 |

每阶段记录版本、任务输入、代码 SHA、测试/评测命令、失败样例、指标和撤回方法。安全违规与错误完成是硬阻断，不用总体成功率掩盖；个人项目没有生产流量时只报告离线结果。

## 关键设计取舍

接受“**通用内核 + 先证明一个垂直场景**”：RepoFix 阶段放 Profile，不放 Runner；第二个资料研究场景用来检验通用性。未选“先建完整插件/多 Agent 平台”（缺真实需求证据）、“让模型决定所有权限”（无法可靠授权与恢复）、“写死 RepoFix 内核”（后续任务污染核心）。阶段 2 若仍需改核心任务分支，应复审 Profile 契约；阶段 1A 若诊断质量不足，不开放写入。这是原 ADR-0001 的现行决定，不再另建 Markdown。

技术跟进采用“公开来源与核验日期 → 固定问题/基线 → 小实验 → 评测 → 修改本页决定 → 代码、测试、学习文档”的顺序。每月复查影响主链路的公开资料，重大安全/版本变化随时登记；只看见流行做法不构成采用理由。

## 仓库维护与 Git 约束

仓库 Markdown **只保留四份**：`README.md` 是 GitHub 自动展示的导航；`学习文档.md` 讲体系知识；本页讲 sonic 产品与工程决定；`面经.md` 独立维护题库、来源和每周变化。跨线用链接，不复制大段正文。新技术事实记原始来源与核验日期；面经候选人自述不冒充官方题库；“已实现”必须链接代码与测试。

每次修改先 `git fetch origin` 并检查工作树，以 GitHub 最新 `main` 为基准；不拿聊天附件或旧导出覆盖远端。使用 `codex/...` 分支做聚焦修改，更新受影响测试和本页能力表；运行适用检查，推送、经 PR/CI 合入，再读取远端提交核对。不得强推 `main` 或覆盖用户历史。推送失败时明确说“仅本地提交、尚未同步 GitHub”。

PR 交代目的、能力状态、测试、文档影响、权限/隐私风险和撤回办法。面经每周更新先核对来源、日期、可信度与去重；自动化只能产生候选或受检验的草稿，不能把传闻直接合入 `main`。无有意义变化可以安静跳过。公开仓库不保存重复源码 ZIP、生成导出或敏感数据；下载包用具体提交归档或带版本的 Release。

这次四文档收敛删除了原先分散的 README、ADR、周报和仓库规则 Markdown，内容按职责吸收到本页、学习文档、面经与导航；旧文件可从 Git 历史恢复。
