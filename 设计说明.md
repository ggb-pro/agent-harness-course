# 设计说明：Sonic v0.1，先做一个自己愿意每天使用的编码助手

Sonic 是个人通用 AI 助手项目。第一站是本地编码：给它一个仓库和具体问题，它能阅读、修改、运行验证，并留下可继续工作的记录。

本文把 **v0.1 的实现决定**写到足以拆成提交的程度；远期方向在末尾保留。文中的 `sonic` 命令、数据表和新接口都是目标规格，当前尚无这个产品 CLI。现有 `sonic_agent 0.5.0` 是旧教学包版本，与新的产品 v0.1 没有版本高低关系。

更新日期：2026-09-18。当前可运行内容与迁移位置见[当前代码与迁移](#十一当前代码与迁移)。

## 阅读导航

- [为什么做 Sonic](#一为什么做-sonic)：个人项目的价值与第一批用户任务。
- [v0.1 做到哪里](#二v01-做到哪里)：明确范围、终端体验、完成标准。
- [完整的一次执行](#三把一次修复从头走到底)：从输入一直到恢复。
- [代码与协议](#四只保留必要的组件) · [模型边界](#五模型接口与消息协议)：模块职责和精确执行顺序。
- [工具和工作区](#六工具工作区与验证)：参数、冲突、测试、撤销。
- [状态和存储](#七状态存储与恢复)：SQLite、崩溃窗口、Session。
- [上下文和交互](#八上下文预算与终端交互)：首版具体做法。
- [开发验收](#十按提交推进而不是一次造完)：纵向切片与发布门禁。
- [演进和维护](#十二向通用助手演进) · [仓库约束](#十三仓库维护约束)。

## 一：为什么做 Sonic

现成编码助手已经很强。Sonic 的第一目标是掌握并持续打磨一个属于自己的执行内核：每个动作怎样产生、模型看到了什么、哪里花了时间、哪里失去了证据，都能读懂、复现和修改。

v0.1 对“强大”的定义很具体：在一个熟悉的 Python 仓库里，能把一个有回归测试的小 Bug 修好，遇到问题时能给出足够证据，让我知道下一步怎么接着做。它不以功能数量和某个竞品持平为验收目标。

三个产品选择由此确定：

1. **行为借鉴 Claude Code。** 用户描述目标，模型决定搜哪些文件、读哪些片段、如何修正；系统负责执行、边界和交付证据。
2. **内核自己实现。** 首个真实 Provider 使用 Anthropic Messages API，工具、会话、上下文和权限都由 Sonic 管理；不把完整第三方 Agent 运行时包一层当成自己的 Harness。
3. **每项能力有可解释的失败。** 编辑冲突、测试没发现、模型断流和上下文不足，各自给出真实原因与恢复办法。

第一 Provider 的选择是为了用一个明确的工具协议完成端到端实现，并不宣称该模型最优。模型 ID 从用户配置读取；第二个 Provider 等第一个真实闭环稳定后再接入。已有 OpenAI 纯文本适配器保留为旧原型素材，产品暂不承诺兼容它的内部接口。

参考资料：Claude Code 的[工作机制](https://code.claude.com/docs/en/how-claude-code-works)用于产品行为；Anthropic 的[工具结果协议](https://platform.claude.com/docs/en/agents-and-tools/tool-use/handle-tool-calls)用于第一适配器。核心运行时不依赖 Claude Code 内部实现。

## 二：v0.1 做到哪里

### 1. 一个明确的第一场景

首批支持用户本人信任、已经能在本机运行测试的 Python Git 仓库。目标任务是：解释现有代码、修复小型 Bug、补测试、实现边界明确的小功能。

输入必须能落到可观察的要求，例如：“折扣金额以分返回，向下取整，修复后运行 pricing 测试。”只有“把这个项目改好”时，先澄清预期行为，不能凭空生成验收条件。

v0.1 的可用能力：

| 用户需要 | 具体实现 |
| --- | --- |
| 查找相关代码 | glob、字面 grep、带行号的 read |
| 修改实现或新增测试 | 单文件唯一文本替换、创建新文件 |
| 验证结果 | 运行用户预先确认的检查别名，如 pricing-tests |
| 看清变化 | 本 Run 编辑清单、before/after diff、检查记录 |
| 保留工作 | SQLite Session，显式 resume，新 Run 接续历史 |
| 停止或拒绝 | Ctrl+C 取消、写入前批准、测试前执行授权 |
| 撤销自己的编辑 | 按单次编辑恢复；发现外部变化时拒绝覆盖 |

跨文件修改通过多次单文件 edit 完成，不承诺一个跨文件原子补丁。首版不开放重命名、删除和任意 Shell 字符串；它们不会妨碍先证明一批小型修复任务，但会限制任务覆盖面。

### 2. 为什么先限制命令入口

测试本身就是执行代码。完全禁用命令会让编码助手无法自证；直接开放任意 Shell 又会同时引入命令解析、后台进程和跨平台行为。

首版取舍是用户在 Sonic 的本地配置中登记检查：

```toml
[checks.pricing]
argv = ["python", "-B", "-m", "unittest", "discover", "-s", "tests", "-p", "test_pricing.py", "-v"]
cwd = "."
timeout_seconds = 60
kind = "unittest"
```

模型只调用 `checks.run({"name":"pricing"})`。执行前由宿主把 `python` 解析为用户确认的绝对可执行路径，并保存解析后的 argv 与命令摘要。模型不能覆盖 cwd、argv、环境变量或 timeout。

项目文件中的命令只能作为建议，不能自动写进授权配置。第一次设置检查时，界面显示实际命令、目录和“会执行项目代码”的说明；批准可以限定为本 Run。依赖安装失败时报告环境未准备，首版不自动安装。

### 3. 首个终端体验

以下是**目标输出示例，当前不可执行**：

```text
$ sonic --workspace ./shop
Sonic · shop · ask · <configured-model>
工作树已有 2 个修改；将保留并记录为初始状态。

> 修复折扣金额放大100倍的问题，运行 pricing 检查

read pricing.py:1-80                         8 lines
checks.run pricing                          2 failed, 1 passed
edit pricing.py                             1 replacement
批准这次修改？[y/n]                          y
checks.run pricing                          3 passed
diff pricing.py                             +1 -1

已调整金额计算并保留向下取整规则。
验证：pricing 的 3 项用例通过，对应当前文件版本。
未验证：全仓测试与非法输入。
session: s_…   run: r_…   checks: passed
```

最终报告把自然语言解释与系统生成的证据栏分开。模型可以解释为什么这样改，但不能把系统的 `not_run` 或 `failed` 改写成绿色通过。

### 4. 明确首版没有承诺的体验

v0.1 提供流式文字和工具状态；用户按 Ctrl+C 停止当前 Run，再输入纠正。运行中异步插话、会话分叉、自动语义压缩、长期后台 Shell、Skills、MCP 和 Subagents 进入 v0.2 以后。

这些差距会显示在 README，而不是藏在“类 Claude Code”这个称呼后面。v0.1 的主要验收是读、改、测、报告和可靠接续。

## 三：把一次修复从头走到底

下面所有步骤都属于一次请求：“修复折扣金额，只修改 pricing.py，运行 pricing 检查”。

### 1. 建立事实

CLI 打开选定的工作区，记录绝对根路径、Git HEAD、初始 tracked diff、untracked 路径及保护范围。已有变更保留，不自动 stash。

Session 持有长期对话；新的 Run 保存本次用户要求、配置版本、预算和允许修改的范围。对文字中无法可靠解析的限制先让用户确认，确认后的结构化范围由执行器检查。

读取项目指令前显示来源；仓库内容不能批准新的目录和命令。项目指令用于代码风格和开发习惯，用户给出的本 Run 限制优先。

### 2. 发送第一次模型请求

ContextBuilder 按顺序装配：系统规则、已确认项目指令、用户目标与限制、可见工具 Schema、既有完整历史。

Provider 把内部消息转换成 Anthropic 格式。UI 可以显示文字增量，但此时没有执行工具。只有整条响应正常结束、调用 ID 唯一且参数 JSON 完整后，才在一个数据库事务里保存完整 assistant frame 和待处理调用。具体工具的 Schema 不符则由执行器返回 invalid_input，保持调用与结果配对。

### 3. 执行工具并回传事实

假设响应中包含两个 read。v0.1 串行执行，每个调用都有自己的 call_id；读取结果带路径、行号、文本和完整文件哈希。

执行器先记录 `tool.started`，完成后把结果和调用状态在一个数据库事务里提交。下一次模型请求只使用已经完成、配对合法的调用和结果。

读取失败也有结果，例如 `not_found`；它可以让模型调整搜索位置，不必让整个 Session 崩溃。

### 4. 修改一次文件

模型提交单文件 edit：path、expected_hash、old_text、new_text。执行器检查修改范围、文件版本和唯一匹配，生成 diff，再按权限模式决定是否等待批准。

批准后重新核对文件版本；保存旧字节与计划新字节；记录 edit intent；原子替换单文件；记录 after_hash 和完成状态。任何一步失败都返回准确阶段。

这些步骤不会阻止用户在另一个编辑器中写文件。v0.1 对自己发起的写入做串行锁，对外部写入使用版本检查和快照；不宣称提供 OS 级并发隔离。

### 5. 验证，必要时继续

运行 checks.pricing 前保存验证输入版本，结束后再次检查相关文件版本、退出码和发现测试数量。若文件在测试期间变化，结果标为 `stale`，即使退出码为 0 也不能使用。

若有失败，把诊断片段作为 Tool Result 返回模型。模型可以继续修改，但仍受 Run 的总预算限制。测试不是循环之外固定只执行一次的末尾步骤。

### 6. 交付与接续

模型提出最终答案后，CompletionChecker 从存储中读取真实 diff 和检查记录。它不发明语义评分，只决定是否还有未完成动作、必需检查是否通过、证据是否过期。

通过时保存 `completed`；存在产物但验证不完整则保存 `partial`，附证据栏。运行终止后会话仍可继续；下一条用户消息创建新 Run。

恢复时先读取上次执行状态并检查工作树，确认事实后再问模型下一步，不直接重复上一个工具调用。

## 四：只保留必要的组件

```text
CLI
 └─ SessionService ──────────────── Store (SQLite + blobs)
      └─ AgentLoop
           ├─ ContextBuilder
           ├─ Provider (Anthropic / Fake)
           ├─ ToolExecutor
           │    ├─ Workspace + Edit checkpoints
           │    ├─ PermissionPolicy
           │    └─ CheckProcess
           └─ CompletionChecker
```

这是本地单进程程序，不按图拆成网络服务。首版依赖选择为 Python 3.11+、asyncio、SQLite、Anthropic 官方 SDK、JSON Schema 校验器、Rich；CLI 参数用标准库 argparse。依赖版本在实现提交中解析并锁定，本文不虚构已验证的版本号。

| 组件 | 输入与输出 | 不应承担的职责 |
| --- | --- | --- |
| SessionService | 用户消息 → 创建 Run、查询和恢复 Session | 选择模型行动 |
| AgentLoop | Run → 模型请求、动作调度、终态 | 文件路径解析、SQL、终端绘制 |
| ContextBuilder | 权威历史与预算 → ModelRequest | 修改原始历史、批准工具 |
| Provider | ModelRequest → 增量事件和完整 ModelTurn | 执行工具 |
| ToolExecutor | 完整调用 → 结构化结果 | 自行推进下一轮模型 |
| Workspace | 路径、版本、编辑 → 内容、diff、receipt | 判定业务需求完成 |
| CheckProcess | 已授权检查定义 → 进程结果与证据 | 接受模型任意命令 |
| CompletionChecker | 当前产物与证据 → 检查状态 | 保证任意程序语义正确 |
| Store | 元数据、frames、工具结果、blobs | 调用外部动作 |

初始文件布局控制在读者可以快速掌握的范围：

```text
src/sonic/
  __init__.py
  cli.py           # 参数、交互、展示
  app.py           # 组装对象和关闭资源
  loop.py          # 唯一模型/工具循环
  protocol.py      # 内部类型
  provider.py      # 协议与 FakeProvider
  anthropic.py     # 首个真实适配器
  context.py
  tools.py         # Registry、参数校验、执行管道
  workspace.py
  checks.py
  policy.py
  session.py
  store.py
  completion.py
```

某个文件出现两个独立变化原因时再拆目录。首版不建空的插件市场、消息总线或 Gateway。

## 五：模型接口与消息协议

### 1. 先保存完整响应，再执行

核心语义如下。这是接口伪代码，`store` 等对象由 app 组装，当前不是可运行脚本：

```python
while budget.can_request():
    request = context.build(session, run)
    draft = await provider.collect_stream(request, on_text=ui.text_delta)
    turn = validate_complete_response(draft)
    store.commit_assistant_and_calls(turn)

    if turn.calls:
        for call in turn.calls:
            if cancel.requested:
                store.cancel_remaining_calls(turn)
                return finish("cancelled")
            if not budget.can_execute_tool():
                store.stop_remaining_calls(turn, reason="budget_exhausted")
                return finish("exhausted")
            result = await tools.execute(call, run)
            store.commit_tool_result(call, result)
        continue

    verdict = completion.inspect(run)
    if verdict.needs_check and budget.can_request():
        store.append_verification_feedback(verdict)
        continue
    return finish(verdict.status)
return finish("exhausted")
```

`collect_stream` 可以不断发 UI 事件，但只有正常响应结束才返回可执行 ModelTurn。若参数 JSON 破损或响应长度截断，该次 attempt 记录为失败，不执行其中看似完整的早期工具。取消或预算停止时，为整批尚未执行的调用补齐 cancelled/error 结果，再组装合法的供应商结果消息；用户后续指令只能在完整配对之后追加。

供应商可能返回文字与工具的混合块。保留原块顺序及原始可重放数据；不要把它们压扁为“最终文字 + 工具名列表”。

### 2. 必须明确的类型

| 类型 | 必需字段 |
| --- | --- |
| ModelRequest | frames、system、tool_specs、model_id、max_output_tokens、request_id |
| ModelTurn | attempt_id、provider_message_id、原始 content blocks、tool_calls、stop_reason、usage |
| ToolCall | call_id、name、经过 Schema 校验的 arguments |
| ToolResult | call_id、ok、error_code、summary、data、artifact_ref、receipt_ref |
| Attempt | request_digest、配置版本、开始/结束时间、provider_request_id、错误类别、usage_known |
| Verdict | run_status、checks_status、evidence_refs、missing_requirements |

模型调用 ID 在 Run 内必须唯一；重复 ID 是协议错误，不能静默复用上次结果。ToolResult 与调用在存储中建立外键关系。

v0.1 每轮最多 8 个工具调用，串行执行。一次响应中包含文字和调用时，文字视为过程说明，只有没有待处理调用且 stop_reason 允许结束，才进入完成判定。

### 3. 第一 Provider 的适配规则

Anthropic 原生 tool_use 对应内部 ToolCall，tool_result 使用正确的供应商消息位置回传。由一个完整 assistant 消息发出的调用，在下一请求前全部获得结果，包括 denied/cancelled/error。

通过 `message_start → content_block_* → message_delta → message_stop` 组装响应；只展示文字增量。停止原因为 `max_tokens` 或流异常时不执行工具，保存 attempt 错误。[流协议](https://platform.claude.com/docs/en/build-with-claude/streaming)

v0.1 默认不启用扩展推理和服务端工具。遇到未支持的内容块明确报 `unsupported_content`，不能丢掉后继续假装消息完整。后续若增加 reasoning 模式，必须先有原始块保留与回放契约测试。

模型 ID、API Key 和上下文能力由用户配置或适配器查询确定。缺配置时停止在 setup，不自动使用别的服务或猜一个模型。

### 4. 重试和预算

SDK 的隐式重试关闭，由运行时统一记账。连接失败、429、可重试 5xx 最多追加 2 次 attempt，指数退避并计入总截止时间。已显示部分文字后断流，UI 标记那一段为不完整，v0.1 停止本 Run，保留接续入口。

模型请求重试不重跑已经完成的 ToolCall。输入相同也不保证模型给出同样调用；去重依据执行记录，不能依据自然语言相似度。

计划默认值如下，均可由用户在可信配置中收紧：

- Run 最多 24 次模型逻辑轮次、60 次工具调用、10 分钟；重试另记 attempt，仍受时间限制。
- 单次输出最多 4096 tokens；累计 token 上限 100000，未知用量记为 unknown 并使用保守估算，不能当 0。
- 单个检查默认 60 秒，最长可配置到 300 秒。
- 模型无工具却缺必要验证时，最多反馈两轮“缺哪些检查”，仍未满足则 partial。

费用只能在模型定价已配置时估算，界面注明估算；v0.1 不承诺准确费用硬封顶。

## 六：工具、工作区与验证

### 1. 工具接口

以下名称和参数作为首版实现契约，不沿用旧 `repo.list/read/search` 名称。

| 工具 | 参数 | 返回关键字段 |
| --- | --- | --- |
| fs.glob | pattern、limit，均可省略 | 相对路径、truncated、scanned |
| fs.grep | query、path、limit | 字面匹配的路径、行号、片段、truncated |
| fs.read | path、start_line、max_lines | 文本、实际行范围、完整文件 hash、truncated |
| fs.edit | path、expected_hash、old_text、new_text | before_hash、after_hash、checkpoint_id、diff |
| fs.create | path、content | after_hash、checkpoint_id；存在则 conflict |
| checks.run | name | argv、cwd、exit_code、discovered、checks_status、log_ref、workspace_digest |
| evidence.read | artifact_id、start_line、max_lines | 当前 Session 已产生的日志片段、范围和截断标记 |

通用执行顺序固定为：查 ToolSpec → JSON Schema 校验 → 参数规范化 → 工作区和策略判断 → 必要审批 → 再校验版本 → 执行 → 结构化记录结果。

允许的字段、默认值和上限同时体现在 Schema 与执行代码中；未知字段拒绝。拒绝或错误也返回结构化结果，避免模型用猜测判断动作是否发生。evidence.read 只接受当前 Session 结果中已有的 artifact_id，不能传磁盘路径或读取其他 Session；输出限制与 fs.read 相同。

### 2. 资源默认上限

这些是首版可调工程默认值，尚未由性能实验优化：

| 资源 | 默认上限 | 超限行为 |
| --- | --- | --- |
| 单个 UTF-8 文本文件 | 1 MiB | 拒绝全文 read/edit，提示缩小对象 |
| 单次 read | 200 行、16 KiB，单行 4 KiB | 返回明确范围与截断标记 |
| 搜索 | 5000 文件、总计 20 MiB、100 命中 | 停止扫描，返回覆盖范围 |
| 单次 edit/create 内容 | 64 KiB | 不执行 |
| 检查 stdout+stderr | artifact 最多 10 MiB | 达上限终止进程，标 output_limit |
| 送给模型的命令输出 | 8 KiB | 头尾与关键错误片段，保留 log_ref |

stdout/stderr 必须边读边计数，不能先无限 capture_output 再截断。工具输出限制先于上下文压缩，避免一条日志就占满窗口。

### 3. 工作区边界

根路径由用户选择，模型参数只接受相对路径。复用并扩充旧路径测试：越界、绝对路径、链接/重解析点、设备文件、敏感凭据和大小限制。Git 的内部目录不作为工具编辑对象。

配置、Session 和快照存放在仓库外的 Sonic 数据目录。v0.1 只处理普通 UTF-8 文本文件，不编辑二进制文件或链接。已有文件的换行格式与 BOM 必须保留，哈希始终基于原始字节。

worktree 可用来隔开版本变化，但它不是沙箱。首版支持用户可信仓库，测试以当前用户权限运行；不提供“可安全执行未知仓库”的产品承诺。

### 4. 编辑和撤销协议

单文件 edit 要求 old_text 恰好出现一次。编辑流程：

1. 获取 Sonic 自己的 workspace 写锁，验证完整字节哈希。
2. 计算新内容和 diff；若需批准，释放运行锁等待用户，批准后重新取锁并检查版本。
3. 把 before 与 planned-after 字节保存成内容寻址 blob，确认落盘。
4. 事务保存 checkpoint 和 edit intent。
5. 再检查当前版本，以同目录临时文件替换目标。
6. 事务记录成功结果及 after_hash；此后 UI 才显示成功。

这无法把文件系统和 SQLite 变成同一事务；故障恢复依赖第七章的哈希对账。同权限外部进程在最后检查与替换之间仍可能竞争，当前保证限于单 Sonic writer 和可检测的版本变化。

创建文件使用排他创建，不能覆盖已有文件；恢复时如果文件内容恰好等于 planned-after，可识别已完成创建。v0.1 不提供模型删除工具，但 `/undo` 可以在用户确认且版本匹配时撤销一次已记录的新建。

撤销仅覆盖 Sonic 的专用编辑工具。测试命令可能生成文件，不能把它们当成已捕获的 edit checkpoint。

### 5. 权限只保留三种模式

- `plan`：只读工具。检查也会执行代码，所以不自动运行。
- `ask`：读自动，每次 edit/create 展示 diff 后询问；checks 依据本 Run 的具体检查授权。
- `edit`：在已确认可写范围内自动编辑；checks 仍需具体授权。

v0.1 不提供 auto/bypass 模式。首版范围小，三种模式已足够个人日常使用。

授权保存 Tool 名称、规范化参数摘要、workspace、Run、资源版本和批准时间。批准前文件变化，原批准失效。配置中的 deny 优先，项目文件和模型输出均不能提升授权。

### 6. 测试证据怎样成立

checks_status 取值为 `passed / failed / not_run / stale / unknown`。

通过需要：对应已要求的 check；退出码 0；没有超时、截断终止或环境错误；支持的 pytest/unittest 适配器确认发现至少一项测试；测试前后验证输入摘要相同。

验证摘要覆盖当前 Git tracked 普通文件和登记的相关 untracked 源码/测试。缓存、.git 与已确认输出目录排除；影响行为的未跟踪配置必须显式登记。测试命令、解释器、检查配置、HEAD 也写入证据。环境状态无法全部穷举，报告保留解释器/依赖环境标识和这项限制。

单看 stdout 中“passed”不算通过。测试解析遇到未知格式时 status=unknown，由用户查看日志；首版适配器测试覆盖无测试、失败、跳过、收集错误和本地化/无法识别输出。

## 七：状态、存储与恢复

### 1. 状态划分

Run 生命周期：

```text
running ↔ waiting_approval
  ├─ completed
  ├─ partial
  ├─ cancelled
  ├─ exhausted
  ├─ failed
  └─ interrupted → 新 Run 在对账后接续
```

工具另有 `pending → started → succeeded/failed/denied/cancelled/unknown` 状态。检查通过与否不是 Run 生命周期：代码解释任务可 completed 且 checks=not_run；用户明确要求修复并测试时，not_run 只能 partial。

模型无工具返回只是“提出结束”，CompletionChecker 负责检查必需证据。不能机械地把所有自然语言任务都要求跑测试。

### 2. SQLite 最小结构

下面是可直接创建的 schema 骨架，用于固定实体关系；实现时的迁移程序负责初始化和版本升级。时间采用 UTC ISO 8601，JSON 字段由应用校验。

```sql
PRAGMA foreign_keys = ON;
CREATE TABLE workspaces (
    id TEXT PRIMARY KEY,
    root TEXT NOT NULL UNIQUE
);
CREATE TABLE sessions (
    id TEXT PRIMARY KEY,
    workspace_id TEXT NOT NULL REFERENCES workspaces(id),
    created_at TEXT NOT NULL
);
CREATE TABLE runs (
    id TEXT PRIMARY KEY,
    session_id TEXT NOT NULL REFERENCES sessions(id),
    status TEXT NOT NULL,
    requirements_json TEXT NOT NULL,
    baseline_json TEXT NOT NULL,
    config_json TEXT NOT NULL
);
CREATE UNIQUE INDEX one_active_run
ON runs(session_id) WHERE status IN ('running', 'waiting_approval');
CREATE TABLE attempts (
    id TEXT PRIMARY KEY,
    run_id TEXT NOT NULL REFERENCES runs(id),
    status TEXT NOT NULL,
    request_ref TEXT NOT NULL,
    result_json TEXT
);
CREATE TABLE frames (
    session_id TEXT NOT NULL REFERENCES sessions(id),
    seq INTEGER NOT NULL,
    run_id TEXT NOT NULL REFERENCES runs(id),
    role TEXT NOT NULL,
    payload_json TEXT NOT NULL,
    PRIMARY KEY(session_id, seq)
);
CREATE TABLE calls (
    run_id TEXT NOT NULL REFERENCES runs(id),
    call_id TEXT NOT NULL,
    name TEXT NOT NULL,
    args_json TEXT NOT NULL,
    status TEXT NOT NULL,
    result_json TEXT,
    PRIMARY KEY(run_id, call_id)
);
CREATE TABLE approvals (
    id TEXT PRIMARY KEY,
    run_id TEXT NOT NULL REFERENCES runs(id),
    call_digest TEXT NOT NULL,
    resource_version TEXT NOT NULL,
    decision TEXT NOT NULL
);
CREATE TABLE edits (
    id TEXT PRIMARY KEY,
    run_id TEXT NOT NULL,
    call_id TEXT NOT NULL,
    path TEXT NOT NULL,
    before_ref TEXT,
    after_ref TEXT NOT NULL,
    status TEXT NOT NULL,
    FOREIGN KEY(run_id, call_id) REFERENCES calls(run_id, call_id)
);
CREATE TABLE checks (
    id TEXT PRIMARY KEY,
    run_id TEXT NOT NULL REFERENCES runs(id),
    name TEXT NOT NULL,
    input_digest TEXT NOT NULL,
    status TEXT NOT NULL,
    evidence_json TEXT NOT NULL
);
```

每个 session 同时只允许一个活动 Run；同一个 workspace 同时只允许一个本机 Sonic writer，通过 OS 文件锁控制，进程退出后锁由 OS 释放。数据库还保留状态，重启必须先识别 interrupted，再创建新 Run。

SQLite 用 WAL、foreign_keys=ON、synchronous=FULL。内容 blob 先落盘，再提交引用它的事务；未引用 blob 可在后续维护中清理。未来 schema_version 比当前程序新时拒绝打开，不能尝试猜着迁移。

### 3. 哪些需要落盘

完整用户消息、完整 assistant frame、工具开始与结果、审批、编辑意图、检查证据、终态必须持久化。UI 的每一个文字 token 不必各自提交 SQLite；它们是临时预览，最终完整响应才成为可重放历史。

原始模型输入包含代码与用户内容，保存在本地受限目录，公开评测导出需另行脱敏。API Key 不进入配置快照和事件。没有启用云同步，换电脑不会自动继承 Session。

### 4. 恢复决策表

| 未完成项 | 检查 | 恢复处理 |
| --- | --- | --- |
| 模型请求断流，工具未启动 | attempt 未完成，calls 无 started | 旧 Run 标 interrupted；新 Run 可重问模型 |
| 审批待决定 | 没有有效 Grant | 取消旧提案，需要时重新提出 |
| edit started，结果缺失 | 当前 hash 等于 planned-after | 对账确认已应用，补结果，不再次编辑 |
| edit started，结果缺失 | 当前 hash 等于 before | 尚未应用，旧动作不自动执行；新 Run 重新规划 |
| edit started，第三种 hash | 当前内容与两者均不同 | 标 unknown，展示冲突，禁止自动覆盖 |
| check started，结果缺失 | 进程身份与本机作业状态 | 不伪造退出码；确认旧进程终止后重新授权执行 |
| 工具结果完整 | frame/call 配对与文件版本 | 重放结果；过期证据标 stale |

Windows 检查进程通过 Job Object 管理；进程创建应先挂入 Job 再开始运行，避免子进程逃离，取消关闭整个 Job。Linux 用独立进程组并在取消后回收。这个能力必须以“父进程生出子进程”的集成用例验证；未通过前，Windows 命令闭环不能标为完成。

不会仅凭数据库中的 PID 杀进程：PID 可能被复用。无法确认所有权时标 unknown，提示用户检查，不执行新的冲突命令。

## 八：上下文预算与终端交互

### 1. 首版构建算法

模型容量来自明确配置或可核验能力信息。设上下文上限 W，预留输出 O，安全余量为 max(2048, 0.1W)，工具定义和必保留规则为 P：

```text
历史与证据预算 = W - O - 安全余量 - P
```

先放系统规则、当前目标、结构化限制和工具 Schema；再放最近完整交互块。已消费的旧工具大正文可以换成 artifact_ref 与摘要。引用的正文保存在本地，可经受控读取工具重新获取。

实际 token 数优先使用 Provider 的计数能力；本地估算必须标记估算并留余量。计数失败且无法确认预算时，停止请求，不把未知当可用。

### 2. v0.1 不做自动语义摘要

首版仅做有界输出与确定性缩减，不用另一个模型反复总结历史。若缩减后仍超预算，保存 Session，以 exhausted 结束并标记 context_limit，提供当前工作记录供用户选择后带入新 Session。同一 Session 的 resume 仍会重新计算预算，不能以新建 Run 假装历史已经变短。

这样能先验证哪些信息必须留下。v0.2 的自动压缩需要单独通过目标/限制保留、工具配对、事实引用和压缩后任务成功率评测。

历史正文缩减必须保持合法消息块，不能留下孤立 tool_result。模型可见视图是存储的派生结果，不覆盖原始 frames。

### 3. CLI 的首版范围

使用 argparse + Rich，先交付普通终端交互，避免在 TUI 布局中消耗过多时间。最少支持：

```text
sonic --workspace PATH
sonic --workspace PATH --prompt "..." --check pricing
sonic resume SESSION_ID
/status
/diff
/undo EDIT_ID
/exit
```

没有交互终端时，待批准动作返回 approval_required 原因，以 partial 结束并补齐未执行调用的结果，不能默认同意。Ctrl+C 请求取消；清理未完成时显示正在结束进程，第二次强制退出会留下 interrupted 状态，恢复按第七章处理。

完成报告的机器格式包含 run_id、status、checks_status、changed_files、evidence、unverified_items。普通终端文本从这个结构生成，方便以后接入其他界面。

## 九：验证设计本身

设计中的每项保证都需要一个会失败的反例。仅运行“正常修复”不能说明下面这些性质成立。

| 保证 | 必须覆盖的反例 |
| --- | --- |
| 流中不提前执行 | 最后一个参数块断流，前面已有看似完整调用 |
| 工具结果合法配对 | 一次两个调用，其中一个 denied，另一个成功 |
| 不覆盖过期文件 | read 后用户修改，审批等待期间用户修改 |
| 通过证据对应当前版本 | 测试后再次编辑、测试执行期间外部文件变化 |
| 预算限制真实有效 | 模型反复返回同一失败命令，重试不断消耗时间 |
| 取消会结束执行 | 测试进程生成长期运行子进程 |
| 恢复不重复编辑 | 分别在 intent、replace、result 三个边界终止进程 |
| 空测试不算成功 | unittest 发现 0 项，pytest 收集失败/全跳过 |
| UI 不能伪造授权 | 渲染、模型文字和仓库内容声称“已经批准” |
| 旧历史不会被压坏 | 正文缩减后每个 call_id 仍有合法对应结果 |

协议测试使用合成 Provider 事件；真实供应商测试单独开启，不能让普通 CI 依赖 API Key。模型做得好不好与 Harness 有没有遵守协议分别评测。

v0.1 发布样本集计划为 20 个提前固定的任务：4 个解释、8 个修 Bug、4 个补测试/小功能、4 个必须拒绝或报告证据不足的任务。开发集与留出集分开，评测前锁定任务和验收脚本。

拟定发布门禁：至少 14/20 完成其预期结果，所有预设禁止动作均未发生，不能把未通过的必需检查标成通过。这是目标阈值，尚无已测成功率；报告必须列出全部失败、模型版本、总成本和耗时。通过 20 项也不代表任意仓库都可靠。

## 十：按提交推进，而不是一次造完

每个提交都要形成可演示的链路，原教学包继续可运行到迁移切换。计划如下：

| 提交 | 实际交付 | 通过证据 |
| --- | --- | --- |
| C1：第一条交互 | sonic 包、最小 CLI、FakeProvider、内存 Store，read→result→final | CLI 能打印工具活动；断流不执行工具 |
| C2：真实模型只读 | Anthropic 工具流、glob/grep/read、范围与 Schema | 合成协议测试；另做有授权的非敏感真实 smoke |
| C3：编辑与批准 | edit/create、ask/plan/edit、单文件 checkpoint、diff | 冲突不覆盖；拒绝不写；新增/已有文件撤销测试 |
| C4：测试闭环 | 检查注册、进程取消、test parser、CompletionChecker | 真实小仓库失败→修复→通过，零测试/超时不通过 |
| C5：持久化接续 | SQLite Store、resume、意图对账、CLI 查询 | 三个编辑崩溃窗口及子进程取消测试 |
| C6：首次发布 | 上下文上限、配置、可重复安装、20 项评测与文档 | Windows/Linux CI、留出任务报告、版本标记 |

C1 的内存 Store 与 C5 的 SQLite Store共享同一接口，因此并非到最后才发明恢复协议；协议从 C1 就按本页定义，持久实现分步加入。禁止同时维护两个产品 Loop。

以下是 C1 的最小接口合同：

```python
class Provider(Protocol):
    async def complete(self, request, on_text) -> ModelTurn: ...

class Store(Protocol):
    def commit_assistant_and_calls(self, run_id, turn) -> None: ...
    def commit_tool_result(self, run_id, result) -> None: ...
    def load_frames(self, session_id) -> list[Frame]: ...

class ToolExecutor(Protocol):
    async def execute(self, call, run_context) -> ToolResult: ...
```

接口伪代码仅表达边界；新增参数必须由真实调用需求驱动。首次发布之前可调整接口，发布以后用迁移和变更说明处理兼容性。

## 十一：当前代码与迁移

本次只重写文档并加入教学实验，未把产品设计宣称为已实现。

| 现有内容 | 真实状态与证据 | 迁移决定 |
| --- | --- | --- |
| 同步 Runner | [runner.py](./代码/sonic/src/sonic_agent/core/runner.py)，FakeProvider 工具循环 | C1 新 Loop 替换；迁移取消、预算测试 |
| Provider | [纯文本 OpenAI 适配器](./代码/sonic/src/sonic_agent/models/openai_responses.py)，明确拒绝 Function Calling | 产品首 Provider 新做；保留脱敏/授权边界测试思路 |
| 仓库工具 | [repository.py](./代码/sonic/src/sonic_agent/capabilities/repository.py)，固定根 list/read/字面 search | C2 移植路径与上限用例，新增新版工具返回值 |
| 工具执行 | [tools.py](./代码/sonic/src/sonic_agent/capabilities/tools.py)，Registry/错误/内存 Journal | 迁移结构化错误；内存 Journal 不作恢复保证 |
| 事件存储 | [events.py](./代码/sonic/src/sonic_agent/core/events.py)，含 SQLite 事务与 revision | 保留测试目的；C5 按新实体迁移，新旧 DB 不静默混用 |
| Context | [context.py](./代码/sonic/src/sonic_agent/core/context.py)，独立选择器，未接 Runner | C6 改成协议合法、超限可停止的构建器 |
| 完成契约 | [completion.py](./代码/sonic/src/sonic_agent/core/completion.py)，固定文本检查 | 不进入产品完成逻辑 |
| 本次课程实验 | [mini_sonic.py](./代码/sonic/examples/mini_sonic.py)，7 个离线场景 | 用于教学，不对外提供产品 API |

包迁移在 C1 增加 `sonic` 与 CLI，暂时让旧 `sonic_agent` 测试继续运行。C2～C5 将有价值的边界用例逐项映射到新模块；C6 确认源码链接、示例和测试全部有新入口后删除旧包。旧历史在 Git 中保留，不复制成另一套 legacy 源码。

完成迁移前，每个提交更新这张表，明确“当前运行入口”和“目标入口”。新产品版本以 `sonic-v0.1.0` 标签发布，避免和旧 Python 包版本混淆。

## 十二：向通用助手演进

后续不是把所有远期模块一次写完，而是用实际问题决定顺序。

**v0.2：把编码体验补完整。** 自动摘要、运行中转向、会话 fork、受控通用命令、长进程与多文件 patch。每项分别说明新失败窗口；已有读改测回归不能退化。

**扩展阶段：需要时引入 Skills、Hooks、MCP 和 Subagents。** 若重复手写排障步骤，先做 Skill；若需要事件上的确定校验，做 Hook；若要访问外部服务，接 MCP；若独立调查确有并行收益，再做 Subagent。不能仅因竞品有某功能就进入 Core。

**第二场景：带来源的技术研究。** 用户给一个技术问题，系统检索、读材料、比较方案并为结论附证据。它复用 Loop、Session、Provider、预算和工具协议，新增 Web 能力与引用检查。若需要在 Loop 内加入大量“如果是研究任务”的分支，应先修正业务与控制流的边界。

**长期目标：跨任务的个人助手。** 等真实需求出现，再增加可纠正的记忆、跨设备入口和定时任务。发邮件、部署、下单等外部效果需要自己的确认和恢复协议，不能沿用“撤销文件”语义。

外部研究遵循一条短流程：记录版本与问题 → 提出 Sonic 的改进假设 → 做最小对照实验 → 写收益和代价 → 决定是否采用。每月做一次资料复核是维护约定，尚不表示已创建定时任务。

本次参考的 nanobot 提交为 [24c41a6](https://github.com/HKUDS/nanobot/blob/24c41a6af0736330cd9c67b3c9d1ec9adeefdf5f/nanobot/agent/runner.py)，DeepSeek Harness 为 [ddefc45](https://github.com/deepseek-ai/deepseek-harness/blob/ddefc45fbc7f8e46dd73185e68295696d1297887/docs/architecture.md)。调用链和学习解释只在[学习文档](./学习文档.md#九带着问题读参考实现)维护，本页记录 Sonic 的选择。

## 十三：仓库维护约束

继续保持四份 Markdown：README 导航与运行入口；学习文档讲机制和可运行实验；设计说明维护决策、范围和当前状态；面经独立维护题库与每周来源更新。面经的周更需求保留，本次不声称已执行全网收集或创建调度。

每次改动先 fetch GitHub main，检查工作树，再在 codex/ 分支做聚焦修改。代码、对应教程、能力状态和验证证据在同一 PR 中更新，经 CI 合入；不强推 main，不拿旧 ZIP 覆盖源码。

改设计先说明哪个失败样本或实际使用反馈触发变更；数字标明是工程默认、发布目标还是实测值。新增文档示例要能运行，接口节选和伪代码明确标注。源码链接优先到文件与符号说明，外部研究使用提交固定链接。

文档质量也有验收：新读者能跑出课程结果并解释一次失败；开发者能按 C1～C6 实施而不临时猜协议；用户能一眼分清已经可用的能力和计划。四文档/链接检查与既有单元测试只能保护结构和回归，不能代替这三项阅读验收。
